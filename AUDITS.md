# AUDITS — project-wide cross-verification registry

> **Compulsory (CLAUDE.md §5):** everything built, in any layer, ships a runnable
> audit that recomputes `done/total` + an integrity check from ground truth
> (DB / live HTTP / real render), enumerating EVERY unit incl. untagged. No task
> is "done", and nothing is committed/deployed, until its audit passes — local
> **and** prod where applicable. The audit's output, not a hand tally, is truth.

## How to run
- Local: `node config/<audit>.mjs` (from `ai-learning-backend/backend`)
- Prod:  `docker exec ailearningpath-api-1 node config/<audit>.mjs`

## Registered audits

| Area | Audit | Verifies |
|---|---|---|
| DSA animations | `config/auditAnimations.mjs` | done/total per pattern (incl. untagged) + 0 broken kinds/steps |
| DSA edge-case catalog | `config/auditEdgeCases.mjs` | done/total per pattern (same 598-problem scope as animations) + 0 malformed (well-formed array ≥2, each entry has non-empty case+handling) |
| Notes / Notebook (GAP #3) | `config/auditNotes.mjs` | conformance scan of every Note doc: required fields, valid enums (scope/kind/type), note→non-empty body, highlight→non-empty quote; 0 malformed. (Backend route also covered by the `note.service` Jest suite + a live HTTP round-trip; frontend by `Notes.test.jsx` render tests.) |
| Pro visual aids | `config/auditVisualAids.mjs` | real-diagram vs text-panel; flags panels whose brief describes a diagram; topics with no visual_aid |
| Java idioms catalog (GAP #7) | `ai-learning-frontend/frontend/scripts/auditIdioms.mjs` (run `node scripts/auditIdioms.mjs` from `ai-learning-frontend/frontend`) | total + done/total per category over the live `src/data/javaIdioms.js`; 0 malformed (every required field non-empty, ≥1 tag, explanation ≥20 chars) + 0 duplicate ids. Frontend render covered by `AudioIdioms.test.jsx`. |
| LLD track (GAP #4) | `config/auditLldTrack.mjs` (`npm run audit:lld`) | enumerates every topic + exercise of `pro_lld` (now **22 modules, 172 topics, 520 exercises** — at AlgoMaster parity): 1 live track, live modules, topics well-formed (hook/teaching.blocks/interviewRelevance/commonGaps), exercises well-formed per type (pattern_match options+correct, predict_output text_match, code_scratch execution+stdout), 0 orphans/dupes, every topic ≥1 exercise, track totals match counts, and every `teaching.visual_aid.svg` (22 pattern topics) is well-formed (balanced tags, `<svg>`-wrapped — 0 malformed). Idempotent (upsert-by-id). **Two further gates:** `npm run acceptance:lld` (live HTTP — all 5 modules reachable, every type 200 not 422, grading correct) and `npm run verify:lld-code` (submits all 10 code_scratch REFERENCE SOLUTIONS through real Judge0 → must all compile + match expected_stdout). |
| Community engine (GAP #8) | `config/auditCommunity.mjs` (`npm run audit:community`) | conformance scan of EVERY `CommunityPost` (user-generated, no fixed denominator): userId+authorName present, valid kind/status, non-empty title+body, well-formed comments, denormalized counts in sync (upvoteCount==upvoters.length, commentCount==comments.length), reports well-formed; malformed must be 0. Full lifecycle via `npm run acceptance:community` (create→list→read→upvote→comment→report→delete, answer-key not leaked). Backend `community.service` Jest (17) + frontend `Community.test.jsx` (6). Seed starters: `npm run seed:community`. |
| System Design track (GAP #2) | `config/auditSysdTrack.mjs` (`npm run audit:sysd`) | enumerates every topic + exercise of `pro_sysd` (now **23 modules, 179 topics, 539 exercises** — exceeds AlgoMaster's 177): live track (slug system-design), topics well-formed (hook/teaching.blocks/interviewRelevance/commonGaps + any visual_aid), exercises well-formed per type (pattern_match options+correct), 0 orphans/dupes, every topic ≥1 exercise, totals match. Live HTTP gate: `npm run acceptance:sysd` (enroll → module → types 200 → flow visual_aid reaches client → grading). Idempotent (upsert-by-id). |
| System Design DEPTH (anti-drift) | `config/auditSysdDepth.mjs` (`npm run audit:sysd-depth`; gate a set with `--require id1,id2`) | enforces **SD_DEPTH_STANDARD.md** — gates on DEPTH, not topic count (the track hit count parity 179>177 while each topic stayed ~500 words / 0 diagrams; a user spotted the gap). Classifies every case-study topic DEEP / PARTIAL / SHALLOW: DEEP needs sections requirements+estimation+hld+api+data_model+bottlenecks + ≥2 deep_dive (via block `section` tags) + a well-formed authored `visual_aid.svg` + ≥1200 words + ≥3 exercises + ≥3 gaps. PARTIAL (started but incomplete) = HARD FAIL. Prints DEEP/total coverage. Coverage: **143/143 DEEP (100%, complete 2026-08-24)** (b1: URL shortener, rate limiter, news feed, chat, KV store; b2: Instagram, video streaming, ride-sharing, web crawler, distributed cache, typeahead; b3: search engine, message queue, leaderboard, notifications, payment, maps; b4: file sync, unique-id, object store/S3, proximity, distributed lock, job scheduler; b5: distributed counter, top-k/trending, CDN, log aggregation, metrics/monitoring, saga orchestrator; b6: API gateway, SSO/IdP, CDC pipeline, service mesh, distributed tracing, webhook delivery; b7: service discovery, schema registry, audit log, multi-tenant SaaS, GDPR deletion, OTP/2FA; b8: vector DB, RAG pipeline, LLM serving, ML model registry/MLOps, load balancer, DNS; b9: DDoS protection, WAF, certificate authority/PKI, KMS, cloud pub/sub, secrets manager; b10: data warehouse, data lake/lakehouse, ETL orchestration, real-time OLAP, anomaly detection, stream processing; b11: container orchestrator, backup/DR, collaborative whiteboard, geospatial DB, blockchain/ledger, image-processing CDN; b12: stock brokerage, photo storage, music streaming, distributed file system, video conferencing, live streaming; b13: IoT platform, multiplayer game server, hot ranking/Reddit, RTB ad exchange, content moderation, ad-click aggregator; b14: Twitter/timeline-fanout, Google Docs/collab-editing, stock exchange/matching-engine, flash sale, online code judge, Airbnb/booking; b15: recommendation system, email/Gmail, ticket booking/Ticketmaster, pastebin, live presence, Slack; b16: time-series DB, multi-region DB, distributed SQL/NewSQL, serverless/FaaS, workflow engine, media processing pipeline; b17: A/B testing, fraud detection, Tinder/matching, comment system, live location sharing, CI/CD pipeline; b18: feature-flag/config service, push notification gateway, social graph/PYMK, shopping cart & checkout, subscription billing, matchmaking; b19: feature store/ML, calendar/scheduling, distributed web cache/reverse proxy, offline-first sync, Q&A platform, distributed graph DB; b20: uptime/status monitoring, two-sided marketplace, package registry, captcha/bot detection, stock market data feed, last-mile delivery routing; b21: developer API platform, e-signature, zero-trust access, webhook ingestion gateway, quota/usage-metering, distributed model training; b22: PII anonymization, geo load balancing/GSLB, plagiarism detection, credit scoring, KYC/identity verification, bulk email/campaign sender; b23: price-comparison aggregator, online learning platform, loyalty/rewards, sportsbook/live-betting, restaurant reservation, helpdesk/ticketing), each render-verified (svg visible) + grading-clean. Seeds: `seed:sysd-depth-pilot`..`-24` (b24 finished the final six: survey/forms, CRM, news aggregator, track-and-trace, insurance claims, crowdfunding). COMPLETE — keep the gate green. |
| LLD DEPTH (anti-drift) | `config/auditLldDepth.mjs` (`npm run audit:lld-depth`; `--require id1,id2`) | enforces **LLD_DEPTH_STANDARD.md** — same depth-not-count gate for pro_lld: DEEP needs sections requirements+entities+design+code+tradeoffs + ≥2 deep_dive + UML-style `visual_aid.svg` + ≥1000 words + ≥3 exercises + ≥3 gaps; PARTIAL = hard fail. Baseline 2026-08-24: **0/129 DEEP** (all legacy-shallow). Deepen in `seed:lld-depth-*` batches like SD. |
| Pro exercise grading (LLD+SD) | `config/auditProGrading.mjs` (`npm run audit:pro-grading`) | verifies every `pattern_match` exercise across `pro_lld`+`pro_sysd` actually GRADES CORRECTLY (beyond structure): testCase type=pattern_match, `testCase.correct === expectedSolution`, the correct answer IS one of the options, ≥2 non-empty unique options, explanation present. 1026/1026 PASS. Catches grader-breaking data bugs the structural track audits don't (per [[project_pro_exercise_grading]]). |
| Listen / audio mode (GAP #5) | `ai-learning-frontend/frontend/src/__tests__/AudioIdioms.test.jsx` (run `npx vitest run src/__tests__/AudioIdioms.test.jsx`) | ListenButton renders, speaks first chunk + shows transport, stop cancels + returns to idle, gathers semantic text from a contentRef (skips button chrome). Pure-client (Web Speech API) so no DB/HTTP layer to audit. |
| Child-view shape (onboarding board/grade dup) | `__tests__/integration/childView.integration.test.js` (run `npm test -- childView.integration`) | pins the POST `_childView` + PUT projection of `/user/children` to carry `tracks`/`activeTrack` against a real in-memory child doc, so a freshly-created (school-track-seeded) child is never seen as un-enrolled — which is what bounced users through /welcome → /start and re-asked board+grade. 3/3 PASS. |
| Mistake Notebook | `__tests__/mistake.service.test.js` (run `npm test -- mistake.service`) | derives the notebook from Attempt records (no new collection): wrong attempt → entry with selected/correct answer, why-wrong from distractor type, 3-day retry date, similar-question IDs (same topic+difficulty, excl. self); latest-correct attempt clears the entry; deleted questions skipped. 4/4 PASS. Route `GET /api/v1/mistakes`, page `/mistakes`, retry via existing `/practice/start-bookmarks`. |
| Today Plan (daily learning queue) | `__tests__/todayPlan.service.test.js` (run `npm test -- todayPlan.service`) | `GET /api/user/today-plan` returns a sequenced queue of REAL question IDs (weak topics + 1 distinct revision topic, prefer-unseen via SeenQuestion), streak status, continue-lesson card, and the daily brief; topics with no playable questions are dropped. 2/2 PASS. Dashboard "Begin plan" launches the queue through the retry-practice flow. |
| Exam readiness score | `__tests__/readiness.service.test.js` (run `npm test -- readiness.service`) | per-subject readiness % = examFrequency-weighted mean of topic accuracy over ALL syllabus topics (uncovered topics drag the score — coverage penalty verified), weakest-3 topics surfaced, JEE/NEET label derived from `User.goal`, null with no attempts. 4/4 PASS. Routes: `readiness` field in `GET /api/v1/analytics-v2/dashboard` + standalone `GET /api/v1/analytics-v2/readiness`; card on Analytics page. |
| Tutor modes + Hinglish | `__tests__/ai.service.test.js` tutor-mode block (run `npm test -- ai.service`) | `tutorModeInstruction`: hint mode forbids revealing answers, socratic mode forbids stating answers, hinglish/hindi language instructions included, default full+en adds nothing; `getChatResponse` threads `{mode, lang}` into the system prompt and stays unchanged without opts. 6/6 PASS. Wired: `POST /api/ai/chat` + doubt chat accept `mode`/`lang` (Joi-validated), stored `User.locale=hi` is finally read as the default language, non-default modes bypass the first-turn response cache, mode/Hinglish chips in the Practice tutor panel. |
| Teacher assignments | `__tests__/assignment.service.test.js` (run `npm test -- assignment.service`) | non-teacher create → 403; teacher creates by class join code with auto-picked topic questions; student completion DERIVED from Attempts on the assigned questionIds after createdAt (pre-assignment attempts ignored, no submission collection); report gated to the owning teacher with per-student attempted/correct/completed rows. 7/7 PASS. Routes: `POST/GET /api/v1/school-group/assignments*`; student card in SchoolGroups (starts via retry-practice flow), teacher form + report in Portal (role-gated). |
| Mastery map (5-state) | `__tests__/masteryMap.service.test.js` (run `npm test -- masteryMap.service`) | every NCERT topic resolves to exactly one of not_started/learning/weak/mastered/needs_revision (weak = 3+ wrong attempts; needs_revision = mastered AND due per the revision engine, name-matched case-insensitively), grouped + sorted by chapter with per-state counts; empty-syllabus → empty map. 2/2 PASS. Route `GET /api/v1/lessons-v2/mastery-map`; clickable colour-grid card on Lessons (each cell opens its topic). |
| Image doubt solving | `__tests__/ai.service.test.js` solveImageDoubt block (run `npm test -- ai.service`) | photo goes to Claude as a base64 image content block + the student's text prompt; reply returned; API error → null (route replies "try a clearer photo"). 2/2 PASS. Route `POST /api/ai/image-doubt` mounted BEFORE the global 100kb express.json (own 6mb parser), Joi-caps base64 ≤5.6MB (jpeg/png/webp), per-user 20/hr limiter + daily AI quota, tutor mode/lang respected; 📷 button in Practice chat downscales client-side to ≤1600px JPEG. |
| Worksheet generator | `__tests__/assignment.service.test.js` generateWorksheet block (run `npm test -- assignment.service`) | non-teacher → 403; teacher gets numbered questions with options, answer key, and solution steps from the bank (zero AI cost); empty topic → 404. 3/3 PASS. Route `POST /api/v1/school-group/worksheet`; Portal form opens a printable page (answer key page-breaks onto its own sheet). |
| AI teacher docs | `__tests__/assignment.service.test.js` generateTeacherContent block (run `npm test -- assignment.service`) | non-teacher → 403; class summary feeds REAL per-student stats (accuracy/attempts/weak/strong from UserProfile) into the prompt — asserted on the actual stats block; parent note requires an existing student in the class (404 otherwise); AI failure surfaces as 503, never an empty doc. 4/4 PASS. Route `POST /api/v1/school-group/teacher-content` (10/hr limiter + daily AI quota); Portal card with kind chips + copy-for-WhatsApp button. |
| Class heatmap | `__tests__/assignment.service.test.js` getClassHeatmap block (run `npm test -- assignment.service`) | students × topics accuracy grid from UserProfile.topicProgress; columns ranked by class coverage (top 12); unattempted cells null (rendered grey, never 0%); teacher-only (403 otherwise). 2/2 PASS. Route `GET /api/v1/school-group/class-heatmap?classCode=`; colour grid card on Portal (green ≥70 / orange ≥40 / red <40 / grey untouched). |
| Career path + scholarships | `__tests__/career.service.test.js` (run `npm test -- career.service`) | every roadmap complete (key, exams, subjects, ≥4 stages, app links into existing surfaces); scholarships grade-filtered (grade-8-only NMMS excluded for a Class-10 user; no grade → full list) with per-user tracked flags; setCareerPath rejects unknown keys, accepts valid + null; toggle uses $addToSet/$pull (no dupes, idempotent); unknown scholarship id → 400. 6/6 PASS. Routes `/api/v1/career/*`; page `/career` + nav entry; `User.careerPath` + `User.trackedScholarships` fields. |
| Offline practice packs | `__tests__/offlinePack.service.test.js` (run `npm test -- offlinePack.service`) | pack targets the user's weakest topics with a client-gradable answer key (correctIndex + joined solution), capped at 8 questions/topic; no practice history → empty pack without querying questions; sync derives correctness + mistake-type SERVER-SIDE from the stored question (client verdict untrusted), skips unknown questions/options, inserts nothing when nothing valid. 5/5 PASS. Routes `GET /api/practice/offline-pack`, `POST /api/practice/sync-offline` (Joi, ≤200 attempts). Mobile: `lib/services/offline_service.dart` (JSON files via path_provider — no new deps), `lib/screens/offline_practice_screen.dart` (download → practice offline → sync badge), entry icon on Practice app bar. `flutter analyze`: 0 errors in lib/. Dart-side: `ai-learning-mobile/test/offline_pack_test.dart` (run `flutter test`) — pack JSON round-trips losslessly, grading fields survive, malformed/corrupt pack degrades to safe defaults instead of crashing. 3/3 PASS (full mobile suite 4/4). |
| Transformations (flashcards / exam Qs / ELI5 / revision / podcast script) | `config/auditTransformations.mjs` | enumerates EVERY NcertTopicContent topic × 5 kinds: cached/total per subject and per kind against the cross-user `AIResponseCache` (`transform::md5(topic::kind::subject::grade)`), and 0 malformed cached payloads (each parses as JSON and matches its kind's shape — podcast additionally needs ≥2 speakers, all lines non-empty). Also enumerates topics with no `name` (untransformable — exit 1). **First live run 2026-08-24: 5385 units, 0 malformed, 0 unnamed — PASS**, after it caught 176 ICSE Math 9/10 topics missing `name`/`chapterNumber` (seed scripts never set them), fixed by `config/backfillIcseTopicNames.mjs` (derives both from topicId; idempotent, re-run 0/0). Read-only/idempotent; exit 1 only on malformed/unnamed (cache fills lazily, so coverage gaps alone don't fail). Service logic: `__tests__/transform.service.test.js` (run `npm test -- transform.service`) — 10/10 PASS: kinds registry, unknown kind → null w/o Claude call, JSON parse + fence-stripping, malformed → null, API error → null, cross-user cache hit (Claude called once), RAG citation attached. Route `POST /api/ai/transform` (auth + Joi + inputGuard + 50/hr limiter + `transformations` flag); UI `TransformChips.jsx` on NcertTopicView. |
| Dialogue podcast player | `ai-learning-frontend/frontend/src/__tests__/PodcastPlayer.test.jsx` (run `npx vitest run src/__tests__/PodcastPlayer.test.jsx`) | 6/6 PASS: collapsed entry renders; fetch → speaks line 1 + full transcript + progress; teacher/student get DISTINCT voices from getVoices() (pitch fallback when device has one); stop cancels + reports listen-through; finishing reports `(linesPlayed, totalLines)` exactly once (no double-report on later stop); fetch failure → error state, nothing spoken. Script integrity (≥2 speakers, non-empty lines) gated by `auditTransformations.mjs`. Listen-through lands in AnalyticsEvent via `POST /api/ai/podcast-listened` (auth + Joi → `trackEvent`, covered by `eventTracker.test.js`). Flag `dialogue_podcast` gates the UI via `GET /api/flags`. |
| Citations (AI answers cite NCERT provenance) | `config/auditCitations.mjs` | enumerates EVERY NcertChunk: labeled/total per subject (chunk has `source` or `chapterTitle`/`conceptName` — i.e. can produce a "📖 From NCERT" label) + chapterNumber coverage; exit 1 if any unlabeled chunk exists (it would ground answers with no citation). **First live run 2026-08-24: 6266/6266 labeled (100% — SocSci 210, English 156, Math 4989, Java 911; chapterNumber 93% Math / 100% rest) — PASS.** Footer logic: citation tests in `__tests__/transform.service.test.js` — line appended from injected-chunk provenance (never model-echoed), absent when RAG found nothing. |
| 2026-08 feature batch (cross-check) | `__tests__/integration/newFeatures.integration.test.js` (run `npm run test:integration -- newFeatures.integration`) | validates the whole batch against a REAL in-memory MongoDB (schema compatibility unit mocks can't catch): mistake notebook derives from real Attempt docs and self-clears on a later correct attempt; today-plan queue returns real playable question ids; assignment lifecycle end-to-end (create by join code → attempt-derived progress → teacher report); offline pack round-trip (answer key out, server-side re-grade in, synced wrong answer lands in the mistake notebook); career path + scholarship tracking persists on the real User doc with live grade filtering. 5/5 PASS. |
| Hindi UI (nav + key CTAs) | `__tests__/navService.test.js` Hindi-locale block (run `npm test -- navService`) | locale=hi translates every school + pro sidebar label server-side (asserted label-by-label against the English nav — no silent English leaks), icon keys and routes untouched; locale=en/absent unchanged. 3/3 PASS. Locale change invalidates the nav cache (`profileV2Service.updateSettings`); both nav routes now select `locale`. Client: `src/i18n.js` `t()` dictionary applied to Dashboard hero CTAs ("Begin plan", commit, "Jump back in"). |

## Adding a new build → add its audit here
Pick the method by type (see CLAUDE.md §5):
- **content / seeds / migrations** → DB-count audit (per-unit counts, 0 malformed, idempotent re-run)
- **backend route / service** → live HTTP round-trip (enrolled-user token) asserting status + shape, + Jest test
- **frontend page / component** → Playwright render asserting the change is visible; for data-driven UI, a DB-derived per-record render check
- **schema / model change** → collection-wide conformance scan (required fields, valid enums)

Write the audit FIRST, register it above, gate "done" on a clean run.
