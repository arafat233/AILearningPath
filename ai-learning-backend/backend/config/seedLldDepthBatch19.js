/**
 * Seed — LLD DEPTH batch 19 (LLD_DEPTH_STANDARD.md).
 *
 *   lld_m20_t3  Design an Event Loop                     (module lld_m20)
 *   lld_m20_t4  Design a CountDownLatch / Barrier        (module lld_m20)
 *   lld_m20_t5  Design a Lock-Free Stack                 (module lld_m20)
 *   lld_m20_t6  Design a Token-Bucket Rate Limiter       (module lld_m20)
 *   lld_m20_t8  Design an Actor System                   (module lld_m20)
 *   lld_m21_t1  Design Blackjack                         (module lld_m21)
 *
 * Verify: node config/auditLldDepth.mjs --require lld_m20_t3,lld_m20_t4,lld_m20_t5,lld_m20_t6,lld_m20_t8,lld_m21_t1
 * Usage:  node config/seedLldDepthBatch19.js  ·  npm run seed:lld-depth-19
 */
import "dotenv/config";
import mongoose from "mongoose";
import { ProModule, ProTopic, ProExercise } from "../models/proModels.js";
import { recomputeTrackTotals } from "./proTrackTotals.mjs";

const TRACK_KEY = "pro_lld";

const FONT = "system-ui,-apple-system,Segoe UI,Roboto,sans-serif";
const esc = (s) => String(s).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
function box(x, y, w, h, label, sub, fill = "#eff6ff") {
  const cy = sub ? y + h / 2 - 3 : y + h / 2 + 5;
  let s = `<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="12" fill="${fill}" stroke="#94a3b8" stroke-width="1.5"/>`;
  s += `<text x="${x + w / 2}" y="${cy}" text-anchor="middle" font-family="${FONT}" font-size="14" font-weight="600" fill="#0f172a">${esc(label)}</text>`;
  if (sub) s += `<text x="${x + w / 2}" y="${y + h / 2 + 14}" text-anchor="middle" font-family="${FONT}" font-size="11" fill="#475569">${esc(sub)}</text>`;
  return s;
}
function arrow(x1, y1, x2, y2, label, dashed) {
  const dash = dashed ? ` stroke-dasharray="5 4"` : "";
  let s = `<line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="#94a3b8" stroke-width="2"${dash} marker-end="url(#ah)"/>`;
  if (label) s += `<text x="${(x1 + x2) / 2}" y="${(y1 + y2) / 2 - 6}" text-anchor="middle" font-family="${FONT}" font-size="10.5" fill="#64748b">${esc(label)}</text>`;
  return s;
}
function note(x, y, text, anchor = "middle") {
  return `<text x="${x}" y="${y}" text-anchor="${anchor}" font-family="${FONT}" font-size="11" fill="#64748b">${esc(text)}</text>`;
}
function svg(w, h, inner) {
  return `<svg viewBox="0 0 ${w} ${h}" xmlns="http://www.w3.org/2000/svg" width="100%" style="max-width:${w}px;height:auto">` +
    `<defs><marker id="ah" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto"><path d="M0,0 L8,4 L0,8 z" fill="#94a3b8"/></marker></defs>` +
    `<rect x="0" y="0" width="${w}" height="${h}" fill="#ffffff"/>${inner}</svg>`;
}

const DIAG = {
  eventloop: svg(860, 250,
    box(20, 30, 180, 56, "EventLoop", "one thread runs run()", "#ede9fe") +
    box(260, 30, 160, 56, "TaskQueue", "macro-tasks, FIFO", "#fef9c3") +
    box(490, 30, 170, 56, "TimerQueue", "min-heap by deadline", "#fce7f3") +
    box(490, 130, 170, 56, "Selector", "select(timeout) on fds", "#f0fdf4") +
    box(260, 130, 160, 56, "MicrotaskQueue", "drained to empty") +
    box(20, 130, 180, 56, "WorkerPool", "blocking / CPU offload", "#eff6ff") +
    box(710, 80, 130, 56, "Callback", "runs to completion", "#f0fdf4") +
    arrow(200, 58, 260, 58, "drains each tick") + arrow(420, 58, 490, 58, "then expired timers") +
    arrow(575, 86, 575, 130, "timeout bounded by") + arrow(490, 158, 420, 158, "readiness enqueues") +
    arrow(260, 158, 200, 158, "offloads blocking work") + arrow(710, 95, 660, 70, "scheduled by") +
    note(430, 232, "The blocking select timeout MUST be min(nearestTimerDeadline - now, idleCap): block longer and every timer fires late; any callback that blocks or burns CPU starves EVERY other callback on the single thread")),
  latchbarrier: svg(860, 260,
    box(20, 30, 180, 56, "CountDownLatch", "ONE-SHOT: count to 0", "#ede9fe") +
    box(260, 30, 165, 56, "count: int", "guarded by monitor", "#fef9c3") +
    box(490, 30, 180, 56, "while (count > 0) wait()", "guarded wait — never if", "#fce7f3") +
    box(490, 140, 180, 56, "notifyAll()", "ALL waiters released", "#f0fdf4") +
    box(260, 140, 165, 56, "CyclicBarrier", "RESETS and reuses") +
    box(20, 140, 180, 56, "generation: Object", "identity of this round", "#eff6ff") +
    box(710, 85, 130, 56, "barrierAction", "run by LAST arriver", "#f0fdf4") +
    arrow(200, 58, 260, 58, "owns") + arrow(425, 58, 490, 58, "waiters block in") +
    arrow(580, 86, 580, 140, "released by") + arrow(490, 168, 425, 168, "also used by") +
    arrow(260, 168, 200, 168, "keyed on") + arrow(710, 100, 670, 70, "then trip()") +
    note(430, 242, "A latch never reopens, so its predicate is count > 0; a barrier reuses the same monitor, so its predicate must be gen == myGeneration — without generation tracking, a fast thread from round N+1 is released by round N's notifyAll")),
  lockfreestack: svg(860, 260,
    box(20, 30, 180, 56, "TreiberStack<E>", "push / pop, no locks", "#ede9fe") +
    box(260, 30, 175, 56, "AtomicReference head", "the ONE mutable word", "#fef9c3") +
    box(495, 30, 175, 56, "compareAndSet(old,new)", "retry loop on failure", "#fce7f3") +
    box(495, 140, 175, 56, "ABA hazard", "same pointer, stale next", "#f0fdf4") +
    box(260, 140, 175, 56, "Node<E>", "value + immutable next") +
    box(20, 140, 180, 56, "AtomicStampedReference", "(pointer, version)", "#eff6ff") +
    box(710, 85, 130, 56, "Backoff", "contention relief", "#f0fdf4") +
    arrow(200, 58, 260, 58, "owns") + arrow(435, 58, 495, 58, "mutated only by") +
    arrow(582, 86, 582, 140, "exposed to") + arrow(495, 168, 435, 168, "corrupts chain of") +
    arrow(260, 168, 200, 168, "fixed by") + arrow(710, 100, 672, 70, "damps CAS storms") +
    note(430, 242, "CAS compares the POINTER, not the history: read head=A, stall, another thread pops A and B and pushes A back — your CAS still succeeds and installs a stale next. Lock-free means SOME thread makes progress, never that yours does")),
  tokenbucket: svg(860, 270,
    box(20, 30, 180, 56, "TokenBucket", "tryAcquire(n) / acquire(n)", "#ede9fe") +
    box(260, 30, 180, 56, "State (immutable)", "tokens + lastRefillNanos", "#fef9c3") +
    box(500, 30, 175, 56, "AtomicReference<State>", "CAS retry, no lock", "#fce7f3") +
    box(500, 145, 175, 56, "refill(now) — LAZY", "elapsed x rate, no timer", "#f0fdf4") +
    box(260, 145, 180, 56, "System.nanoTime()", "monotonic, NOT wall clock") +
    box(20, 145, 180, 56, "StripedBucketRegistry", "per-key + eviction", "#eff6ff") +
    box(720, 90, 120, 56, "Waiter", "computed sleep", "#f0fdf4") +
    arrow(200, 58, 260, 58, "holds one") + arrow(440, 58, 500, 58, "swapped by") +
    arrow(587, 86, 587, 145, "computed via") + arrow(500, 173, 440, 173, "reads clock from") +
    arrow(260, 173, 200, 173, "one bucket per key") + arrow(720, 105, 677, 75, "blocks on deficit") +
    note(430, 252, "Tokens are NEVER added by a timer thread: refill is derived on read as min(capacity, tokens + elapsedNanos x ratePerNano). Check-and-consume must be ONE atomic step — CAS the whole (tokens, lastRefillNanos) snapshot or two threads both see the last token")),
  actorsystem: svg(880, 270,
    box(20, 30, 180, 56, "ActorSystem", "spawn / stop / registry", "#ede9fe") +
    box(265, 30, 175, 56, "ActorRef", "the ONLY handle — tell()", "#fef9c3") +
    box(505, 30, 175, 56, "Actor", "PRIVATE state + receive()", "#fce7f3") +
    box(505, 145, 175, 56, "Mailbox", "bounded queue + policy", "#f0fdf4") +
    box(265, 145, 175, 56, "Dispatcher", "N actors onto M threads") +
    box(20, 145, 180, 56, "Supervisor (parent)", "restart / stop / escalate", "#eff6ff") +
    box(730, 90, 130, 56, "Message", "immutable value", "#f0fdf4") +
    arrow(200, 58, 265, 58, "hands out") + arrow(440, 58, 505, 58, "hides the") +
    arrow(592, 86, 592, 145, "enqueues into") + arrow(505, 173, 440, 173, "scheduled by") +
    arrow(265, 173, 200, 173, "children of") + arrow(730, 105, 683, 75, "only thing that crosses") +
    note(440, 252, "State is private and ONLY messages cross the boundary, so there are no locks by construction — the dispatcher's invariant that ONE actor processes at most ONE message at a time is what makes that state safe. Block inside receive() and you steal a shared pool thread")),
  blackjack: svg(880, 280,
    box(20, 30, 180, 56, "BlackjackGame", "round state machine", "#ede9fe") +
    box(265, 30, 175, 56, "Player", "bankroll + 1..N hands", "#fef9c3") +
    box(505, 30, 175, 56, "Hand", "cards, bet, isSplit", "#fce7f3") +
    box(505, 150, 175, 56, "HandValue", "(hard, soft) — Ace = 1 or 11", "#f0fdf4") +
    box(265, 150, 175, 56, "Shoe", "N decks + cut card") +
    box(20, 150, 180, 56, "DealerStrategy", "hit-to-17, soft-17 flag", "#eff6ff") +
    box(730, 95, 130, 56, "Payout", "3:2 vs 1:1", "#f0fdf4") +
    arrow(200, 58, 265, 58, "seats") + arrow(440, 58, 505, 58, "splits into more") +
    arrow(592, 86, 592, 150, "valued by") + arrow(505, 178, 440, 178, "dealt from") +
    arrow(265, 178, 200, 178, "dealer driven by") + arrow(730, 110, 683, 75, "per HAND, not per player") +
    note(440, 262, "NEVER accumulate a running int: an Ace is 1 OR 11, so compute (hard, soft) and pick the best <= 21. A split turns one Player into N independently resolved Hands, so bets, payouts and busts are all per-HAND")),
};

const TT = (moduleId, id, num, name, slug, tags, hookQ, hookI, blocks, rel, gaps, diff, svgStr, vaType) => ({
  trackKey: TRACK_KEY, moduleId, topicId: id, topicNumber: num, name, slug,
  metadata: { estimated_minutes: 50, difficulty: 4, prerequisites: [], tags },
  hook: { question: hookQ, insight: hookI },
  teaching: { blocks, visual_aid: { type: vaType, svg: svgStr, alt_text: `${name} class diagram.` } },
  interviewRelevance: rel, commonGaps: { gaps },
  prerequisites: [], estimatedMinutes: 50, difficulty: diff, xpReward: 80, visualizer: null,
});
const C = (section, heading, body) => ({ kind: "concept", section, heading, body });
const K = (section, heading, body) => ({ kind: "code", section, heading, body });
const G = (id, w, r, d) => ({ gap_id: id, what_students_get_wrong: w, remediation: r, detection_pattern: d });

const TOPICS = [
  // ─────────────────── EVENT LOOP ───────────────────────────────────────────
  TT("lld_m20", "lld_m20_t3", 3, "Design an Event Loop", "design-event-loop",
    ["case-study", "concurrency", "event-driven", "scheduling"],
    "Your gateway holds 50,000 idle WebSocket connections. The thread-per-connection version needs 50,000 stacks — about 50 GB of reserved stack space — and the scheduler spends more time context-switching than working, even though at any instant maybe 200 sockets have data. Someone proposes a single thread that services all of them. That sounds absurd until you notice each connection is idle 99.6% of the time. Design that single-threaded loop — and then answer the two questions that break naive versions: why do my timers fire 80 ms late, and why did one slow callback freeze all 50,000 clients?",
    "An event loop is not magic concurrency; it is one thread executing a strict cycle over THREE sources of work — a task queue, a timer queue ordered by deadline, and an I/O readiness selector — and its entire correctness rests on a single number: the timeout it passes to the blocking select call. That timeout must be the time until the NEAREST timer deadline, never a fixed constant, because the thread is asleep inside select and nothing else can fire a timer. Get that number wrong and timers drift; get it right and the loop sleeps exactly as long as it can with zero busy-waiting. The second load-bearing rule is the flip side of having one thread: every callback runs to completion, so any callback that blocks or burns CPU delays every other callback in the system — which is why the design must include an escape hatch (a worker pool) and a safe way to hand results back into the loop.",
    [
      C("requirements", "Requirements",
        "Functional: (1) submit(Runnable) — enqueue work to run on the loop thread, callable from ANY thread; (2) setTimeout(delayMs, Runnable) and setInterval — run a callback at or after a deadline, returning a handle that supports cancellation; (3) register(channel, interestOps, handler) — express interest in I/O readiness on a socket and receive a callback when it is readable/writable; (4) a run() method that owns the calling thread until stop() is requested; (5) a microtask facility so a callback can schedule follow-up work that runs BEFORE the next macro-task. Non-functional constraints that actually shape the design: the loop must be IDLE-CHEAP — a loop with no ready work and a timer 3 seconds out must consume approximately zero CPU, which rules out any spin or polling design and forces a blocking wait with a computed timeout. Timer accuracy is a stated requirement with an honest bound: callbacks fire AT OR AFTER their deadline, never before, with drift under normal load in the low single-digit milliseconds — an event loop cannot promise hard real-time and saying so is part of the design. Cross-thread submission must be safe and must WAKE a loop currently blocked in select, otherwise a submitted task sits until the next unrelated event. Fairness: no source of work may starve the others indefinitely. Explicitly out of scope: multi-core scaling inside one loop (the answer is N loops, one per core, each with its own sockets), and pre-emption of a running callback — the run-to-completion model is a deliberate constraint, not an oversight, and the requirements section is where you state that consequence before the interviewer finds it."),
      C("entities", "Core entities & responsibilities",
        "EVENTLOOP is the orchestrator and the owner of the loop thread. It holds the three work sources, records its own thread identity (so inLoopThread() can assert that callbacks never touch loop state from outside), and exposes submit/setTimeout/register/run/stop. TASKQUEUE is a thread-safe FIFO of macro-tasks — in Java a ConcurrentLinkedQueue or an ArrayDeque guarded by a lock — and it is the ONLY structure in the design written to by foreign threads, which usefully concentrates all cross-thread reasoning into one object. TIMERQUEUE is a MIN-HEAP (PriorityQueue) of TimerTask ordered by deadline nanos, with a tie-break sequence number so two timers with the same deadline fire in registration order; it must support O(log n) insert, O(1) peek of the earliest deadline (the number the loop needs every iteration), and cancellation. TIMERTASK is the handle returned to callers: deadline, the Runnable, a repeat interval or zero, and a cancelled flag — cancellation flips the flag rather than removing from the heap, because O(n) removal from a PriorityQueue on every cancel is how a chat server with 50,000 heartbeat timers dies; the loop skips cancelled tasks when they surface. SELECTOR wraps the OS readiness primitive (epoll/kqueue, java.nio.channels.Selector) and answers 'which of these registered channels can be read or written right now, and wait at most T for the answer'. CHANNELHANDLER is the per-socket callback bundle (onReadable, onWritable, onError). MICROTASKQUEUE is a plain non-thread-safe ArrayDeque owned solely by the loop thread, drained to empty after each macro-task. WORKERPOOL is an ExecutorService that is NOT part of the loop but is named in the design as the mandatory escape hatch for blocking or CPU-heavy work."),
      C("design", "Class design: the cycle, and the ONE number that makes it correct",
        "The design is a single method whose body is a fixed cycle, and the whole interview lives inside that cycle's ordering. Each iteration: (1) compute timeout = clamp(earliestDeadline - now, 0, idleCap), or idleCap if no timers exist; (2) if the task queue is already non-empty, force timeout = 0 so we do not sleep on work we already have; (3) selector.select(timeout) — this is where an idle loop spends 99% of its time, blocked in the kernel at zero CPU; (4) drain ready I/O keys, invoking each handler; (5) drain EXPIRED timers — pop from the heap while peek().deadline <= now, skipping cancelled ones, rescheduling repeating ones; (6) drain the task queue, taking a SNAPSHOT of its current size first so tasks submitted by tasks run next tick rather than starving the other sources; (7) after every single callback in steps 4-6, drain the microtask queue to empty. Patterns, named honestly: REACTOR is the architectural pattern this IS — a demultiplexer (selector) dispatching events to registered handlers — and calling it by name is worth credit because it tells the interviewer you know the loop is not a novel invention. OBSERVER describes the handler registration. COMMAND describes the queued Runnable. What is deliberately absent: no thread pool inside the loop, no synchronized blocks around loop state. That second absence is the design's whole payoff — because exactly one thread ever touches the timer heap, the selector, and the microtask queue, none of them need locks, and an entire class of race conditions cannot occur. The single lock-free-by-construction seam is the task queue, and cross-thread submission must therefore do exactly two things: offer to the concurrent queue, then call selector.wakeup() to break the blocking select. Forgetting the wakeup is the most common structural bug in a hand-rolled loop, and it presents as 'my task ran 30 seconds later, when some unrelated socket happened to become readable'."),
      K("code", "Core classes (Java)",
        "final class EventLoop implements Executor {\n  private final Selector selector = Selector.open();\n  private final Queue<Runnable> tasks = new ConcurrentLinkedQueue<>();      // written by ANY thread\n  private final ArrayDeque<Runnable> microtasks = new ArrayDeque<>();       // loop thread ONLY\n  private final PriorityQueue<TimerTask> timers =\n      new PriorityQueue<>(Comparator.comparingLong((TimerTask t) -> t.deadline).thenComparingLong(t -> t.seq));\n  private final AtomicLong seq = new AtomicLong();\n  private volatile Thread loopThread;\n  private volatile boolean running = true;\n  private static final long IDLE_CAP_MS = 1000;   // wake occasionally even with no timers\n\n  EventLoop() throws IOException {}\n\n  boolean inLoopThread() { return Thread.currentThread() == loopThread; }\n\n  @Override public void execute(Runnable task) {          // safe from ANY thread\n    tasks.offer(task);\n    if (!inLoopThread()) selector.wakeup();                // break the blocking select — NOT optional\n  }\n\n  void runMicrotask(Runnable r) {                         // loop thread only\n    assert inLoopThread();\n    microtasks.add(r);\n  }\n\n  TimerTask setTimeout(long delayMs, Runnable r) { return schedule(delayMs, 0, r); }\n  TimerTask setInterval(long periodMs, Runnable r) { return schedule(periodMs, periodMs, r); }\n\n  private TimerTask schedule(long delayMs, long periodMs, Runnable r) {\n    TimerTask t = new TimerTask(System.nanoTime() + delayMs * 1_000_000L,\n                                periodMs * 1_000_000L, seq.getAndIncrement(), r);\n    if (inLoopThread()) timers.add(t);\n    else execute(() -> timers.add(t));                    // heap is loop-thread-confined\n    return t;\n  }\n\n  void run() throws IOException {\n    loopThread = Thread.currentThread();\n    while (running) {\n      long timeout = computeTimeout();                    // THE load-bearing number\n      if (timeout == 0) selector.selectNow(); else selector.select(timeout);\n      processIo();\n      processExpiredTimers();\n      processTasks();\n    }\n  }\n\n  private long computeTimeout() {\n    if (!tasks.isEmpty()) return 0;                       // work already pending — do not sleep\n    TimerTask next = timers.peek();\n    if (next == null) return IDLE_CAP_MS;\n    long ms = (next.deadline - System.nanoTime()) / 1_000_000L;\n    return Math.max(0, Math.min(ms, IDLE_CAP_MS));        // never sleep past the nearest deadline\n  }\n\n  private void processIo() {\n    Set<SelectionKey> keys = selector.selectedKeys();\n    for (Iterator<SelectionKey> it = keys.iterator(); it.hasNext(); ) {\n      SelectionKey k = it.next(); it.remove();            // MUST remove — the set is not auto-cleared\n      ChannelHandler h = (ChannelHandler) k.attachment();\n      try {\n        if (k.isReadable()) h.onReadable(k);\n        if (k.isValid() && k.isWritable()) h.onWritable(k);\n      } catch (Throwable e) { h.onError(e); }             // a throwing handler must NOT kill the loop\n      drainMicrotasks();\n    }\n  }\n\n  private void processExpiredTimers() {\n    long now = System.nanoTime();\n    while (true) {\n      TimerTask t = timers.peek();\n      if (t == null || t.deadline > now) break;\n      timers.poll();\n      if (t.cancelled) continue;                          // lazy cancellation — no O(n) heap removal\n      safeRun(t.job);\n      if (t.periodNanos > 0 && !t.cancelled) {\n        t.deadline = now + t.periodNanos;                 // fixed-DELAY: no catch-up storm\n        timers.add(t);\n      }\n    }\n  }\n\n  private void processTasks() {\n    int budget = tasks.size();                            // snapshot: tasks queued by tasks wait a tick\n    for (int i = 0; i < budget; i++) {\n      Runnable r = tasks.poll();\n      if (r == null) break;\n      safeRun(r);\n    }\n  }\n\n  private void safeRun(Runnable r) {\n    try { r.run(); } catch (Throwable e) { onUncaught(e); }\n    drainMicrotasks();                                    // microtasks run BEFORE the next macro-task\n  }\n\n  private void drainMicrotasks() {\n    Runnable m;\n    int guard = 100_000;                                  // a microtask that re-queues itself is a hang\n    while ((m = microtasks.poll()) != null && guard-- > 0) {\n      try { m.run(); } catch (Throwable e) { onUncaught(e); }\n    }\n  }\n\n  private void onUncaught(Throwable e) { e.printStackTrace(); }\n  void stop() { running = false; selector.wakeup(); }\n}\n\nfinal class TimerTask {\n  long deadline; final long periodNanos, seq; final Runnable job; volatile boolean cancelled;\n  TimerTask(long d, long p, long s, Runnable j) { deadline = d; periodNanos = p; seq = s; job = j; }\n  void cancel() { cancelled = true; }                     // O(1); the heap skips it when it surfaces\n}\n\ninterface ChannelHandler {\n  void onReadable(SelectionKey k) throws IOException;\n  void onWritable(SelectionKey k) throws IOException;\n  void onError(Throwable t);\n}"),
      C("deep_dive", "Deep dive: the select timeout is the timer subsystem",
        "This is the mechanism that carries the whole design, and it is the one most candidates get wrong on the first pass. The naive loop is written as: select for a fixed 100 ms, then check timers. It looks harmless and it is broken in two directions at once. First, ACCURACY: a timer scheduled for 5 ms from now cannot fire until the select returns, so it fires up to 100 ms late — and the reason is not a bug in the timer code but the fact that the thread was ASLEEP IN THE KERNEL and no other thread exists to wake it. Second, IDLE COST: with no work at all, a 100 ms select wakes ten times a second forever, which on a phone or a laptop is measurable battery burn and on a server with a thousand loops is a thousand wakeups a second doing nothing. The correct rule is that the timeout is DERIVED, not configured: timeout = earliestDeadline - now, clamped to at least zero (a deadline already past means do not sleep at all) and at most an idle cap (so the loop still wakes occasionally to observe a stop flag or a clock jump). Once the loop is written that way, three subtleties follow and the interviewer will probe each. (a) If the task queue is non-empty at the top of the cycle, the timeout MUST be forced to zero — otherwise the loop sleeps until a timer while a submitted task sits ready. (b) A foreign thread that enqueues work while the loop is blocked must call selector.wakeup(), which makes an in-progress select return immediately; wakeup() is also 'sticky' — a wakeup issued while no select is in progress causes the NEXT select to return at once, which is exactly the memory you want, because otherwise there is a lost-wakeup race between offer() and select(). (c) A new timer scheduled from a foreign thread must go through the task queue rather than touching the heap, both because the heap is unsynchronized and because the enqueue path already performs the wakeup that recomputes the timeout. Finally, use a MONOTONIC clock (System.nanoTime) for deadlines and never System.currentTimeMillis: if an NTP correction steps the wall clock backwards by two seconds, every wall-clock deadline is suddenly two seconds in the future and every timer in the system hangs. The heap gives O(log n) insert and O(1) peek, so computing this number costs nothing even with a hundred thousand timers registered."),
      C("deep_dive", "Deep dive: run-to-completion, starvation, and the worker-pool escape hatch",
        "The single-threaded model buys you freedom from locks, and it charges for it with one hard rule: THE LOOP CANNOT PRE-EMPT A CALLBACK. Whatever a callback does, it does with the loop stopped. A callback that runs a 400 ms JSON parse over a 50 MB payload adds 400 ms of latency to every pending timer, every ready socket, and every queued task in the process — for 50,000 connections that is one user's request freezing everyone. This is not a tuning problem; it is the defining property of the architecture, and the design must answer it explicitly rather than hope callbacks behave. Three concrete offenders, in the order they appear in real code: (1) SYNCHRONOUS I/O — a JDBC call, a file read, a DNS lookup, or worst of all a Future.get() on the loop thread. Blocking on the loop thread converts an asynchronous server into a serial one, and blocking on a future completed BY a task queued on the same loop is a genuine self-deadlock the loop can never escape. (2) CPU-BOUND work — compression, cryptography, template rendering, big sorts. (3) UNBOUNDED loops in a callback, including the pathological microtask that re-enqueues itself and prevents the loop from ever reaching the next macro-task (the guard counter in drainMicrotasks exists for exactly that). The remedy is a WORKER POOL and a disciplined round trip: the callback packages the blocking work, submits it to an ExecutorService, and returns immediately; the worker computes off-loop; and the worker hands the result back by calling loop.execute(() -> continuation(result)) — never by touching loop-owned state directly, because the entire lock-free argument depends on the confinement holding. That round trip is the design's answer to 'how do you use more than one core': you do not make the loop multi-threaded, you keep it single-threaded and push the parallelism into pools and into multiple loops. Two more starvation controls belong in the design. A TASK BUDGET per iteration (process the snapshot size, not until empty) prevents a self-feeding task queue from monopolising the cycle and stalling I/O and timers. And an I/O budget — at very high connection counts, capping ready-key processing per iteration keeps the timer queue responsive. State the observability hook too: measure loop LAG (actual iteration start minus expected) and alarm on it, because lag is the single metric that reveals a misbehaving callback in production, and naming it signals you have operated one of these, not just written one."),
      C("deep_dive", "Deep dive: macro-tasks, microtasks, and ordering guarantees",
        "The two-queue split confuses people until you state the purpose plainly: MACRO-TASKS are the loop's unit of scheduling, and MICROTASKS are the mechanism for 'finish this logical operation before anything else happens'. After each macro-task — one I/O handler, one expired timer, one submitted task — the loop drains the microtask queue COMPLETELY, including microtasks queued by microtasks, before it touches another macro-task. In JavaScript this is exactly why promise callbacks (microtasks) all run before the next setTimeout callback (a macro-task), and why the classic ordering puzzle resolves as sync-code, then promises, then timers. The design reason is atomicity of continuation: when a promise resolves, its dependent callbacks should observe the world as it was when it resolved, not interleaved with an unrelated socket read that mutates shared state. Because both run on the same thread, that guarantee is free — no lock is needed to get it. The costs must be stated honestly. Microtasks can STARVE macro-tasks: a microtask that always enqueues another microtask means the drain loop never terminates, and the whole process hangs with no I/O progress and no timers firing — a real class of production incident, which is why the code carries a guard and real runtimes either cap the drain or accept the hang as a programmer error. The reverse — macro-tasks starving microtasks — cannot happen, since the drain is unconditional after every macro-task. Now the ORDERING guarantees the design does and does not offer, since interviewers ask for them precisely. Within one source, order is FIFO: tasks submitted by a single thread run in submission order, and timers with equal deadlines run in registration order thanks to the sequence tie-break. ACROSS sources there is no global order — a timer due at the same instant as a socket becoming readable can run in either order, and callers must not depend on it. Between two DIFFERENT submitting threads there is no order at all beyond what their own synchronization establishes. And the guarantee people most often assume but never actually get: setTimeout(f, 0) does not mean 'run f now'. It means 'run f on a future iteration, at or after now', which is strictly weaker and strictly later than a microtask. Being able to distinguish those three levels — microtask, zero-delay macro-task, and timer — is what separates someone who has used an event loop from someone who has designed one."),
      C("tradeoffs", "Trade-offs & extension points",
        "EVENT LOOP vs THREAD-PER-CONNECTION: the loop wins decisively when connections are numerous and mostly idle (chat, push, proxies, API gateways), because memory is per-connection state (a few KB) rather than per-connection stack (512 KB to 1 MB reserved) and there are no context switches between connections. Thread-per-connection wins when work per request is CPU-heavy and connection counts are low: the code is straight-line, debuggable, and stack traces are meaningful, whereas the loop's traces are amputated at every async boundary. VIRTUAL THREADS (Java 21+, Loom) genuinely change this calculus and a current candidate should say so: they give the thread-per-connection PROGRAMMING MODEL with event-loop-like memory characteristics, so 'just write blocking code on a virtual thread' is now a legitimate answer — with the caveats that a virtual thread pinned inside a synchronized block or a native call still consumes a carrier thread, and that the loop remains the right model when you need explicit control over scheduling and ordering. ONE LOOP vs N LOOPS: a single loop uses one core, full stop. The standard scaling answer is N loops, one per core, each owning a disjoint set of sockets — Netty's EventLoopGroup, nginx workers, SO_REUSEPORT accept distribution — which preserves the no-locks property WITHIN a loop while using the whole machine; the cost is that cross-loop communication is now message passing, and a hot connection can imbalance the loops. SINGLE QUEUE vs PRIORITY LANES: adding priority to the task queue lets control-plane work (shutdown, health checks) jump ahead of bulk work, at the cost of possible starvation of the low lane and a heavier queue. TIMER WHEEL vs MIN-HEAP: the heap is O(log n) insert and simple; a HASHED TIMING WHEEL is O(1) insert and expiry and is what you propose when the workload is 'a million connections each with a reset-on-activity idle timeout', where the heap's per-reschedule cost dominates — the trade is coarser resolution equal to one tick. WHAT BREAKS AT SCALE: any single long callback (the top production failure mode, monitored via loop lag); unbounded queues turning back-pressure into an OutOfMemoryError, so the task queue should be bounded with a defined rejection policy; and cancellation debt, since lazily cancelled timers stay in the heap until their deadline passes — with millions of cancels, add a periodic compaction that rebuilds the heap when the cancelled fraction exceeds a threshold. EXTENSIONS worth naming: a Promise/Future layer built on top so users compose rather than nest callbacks, per-callback timing instrumentation, and a graceful-shutdown protocol that stops accepting, drains queues with a deadline, then closes."),
    ],
    "The event loop is a favourite senior systems question because it forces you to reason about a scheduler rather than use one. Interviewers typically start with 'how would you serve 50,000 idle connections on one thread', then drive to three specific probes. First, the select timeout: they ask why your timers are late, and the answer must be that the blocking wait's timeout is derived from the nearest deadline, plus the wakeup() call that makes cross-thread submission visible immediately. Second, run-to-completion: they describe a slow callback and expect you to say that it stalls the entire loop, then to propose the worker-pool round trip with the result re-entering via execute(). Third, ordering: they will ask what runs first among a microtask, setTimeout(f, 0), and an I/O callback, and expect precise language about macro versus micro drain and the absence of any cross-source guarantee. Strong candidates also name the model honestly — Reactor pattern, one loop per core to use the machine, monotonic clocks for deadlines, and virtual threads as the modern alternative that may make the whole design unnecessary for their use case.",
    [
      G("fixed_select_timeout", "Passing a constant timeout (or 0) to the blocking select, then checking timers afterwards — so timers fire up to one poll interval late, and an idle loop burns CPU spinning.", "Derive the timeout every iteration: timeout = clamp(earliestTimerDeadline - now, 0, idleCap), and force it to 0 when the task queue is already non-empty. Peek the min-heap for the deadline — it is O(1).", "selector.select(100) or a while(true){ selectNow(); } spin; timer drift proportional to the poll interval; an idle process at a steady few percent CPU."),
      G("missing_wakeup_on_submit", "Enqueuing a cross-thread task with queue.offer(task) and nothing else, so the task waits until some unrelated event happens to return the blocked select.", "Every foreign-thread submission must offer to the concurrent queue and then call selector.wakeup(); wakeup is sticky, so it also covers the race where the submit lands just before the loop enters select.", "Tasks that execute seconds late but only when the server is idle; latency that mysteriously improves under load; no wakeup/notify call anywhere in the submit path."),
      G("blocking_inside_a_callback", "Doing a JDBC query, a file read, or a Future.get() inside a callback because 'it is only a few milliseconds', which serialises every other connection behind it.", "Treat the loop thread as sacred: offload blocking or CPU-heavy work to an ExecutorService and return the result by calling loop.execute(() -> continuation(result)); add loop-lag monitoring to catch violators in production.", "Synchronous driver calls or Thread.sleep inside handlers; p99 latency that tracks the slowest endpoint across all endpoints; a self-deadlock when get() waits on a future completed by the same loop."),
    ],
    0.6, DIAG.eventloop, "Event loop class diagram"),

  // ─────────────────── LATCH / BARRIER ──────────────────────────────────────
  TT("lld_m20", "lld_m20_t4", 4, "Design a CountDownLatch / Barrier", "design-latch-barrier",
    ["case-study", "concurrency", "synchronizer", "monitor"],
    "Two requirements land in the same sprint. One: the service must not accept traffic until the config loader, the cache warmer, and the connection pool have all finished — a single gate that opens once and stays open. Two: a physics simulation runs eight worker threads over 10,000 timesteps, and no worker may start step k+1 until all eight have finished step k — a gate that must open and close 10,000 times. A candidate who builds one primitive for both ships a bug that appears about once every few thousand rounds and cannot be reproduced on demand. Design both, and name precisely what makes the second one harder.",
    "The difference between a latch and a barrier is not the API, it is REUSE, and reuse is what forces the hard mechanism. A latch counts down to zero once and stays open forever, so a thread that arrives late simply sails through — the state is monotonic and there is nothing to get wrong beyond using a while loop instead of an if. A cyclic barrier resets its count after every round, which means a single notifyAll() from round N can wake a thread that has already lapped into round N+1 and is waiting for a completely different trip. The fix is GENERATION tracking: every waiter remembers the generation object it arrived in, and its wait predicate is not 'count > 0' but 'my generation is still the current one'. Generations, the guarded while loop, and notifyAll instead of notify are the three things that make a reusable synchronizer correct.",
    [
      C("requirements", "Requirements",
        "Two primitives with deliberately different contracts. COUNTDOWNLATCH: constructed with a non-negative count N. countDown() decrements toward zero and is a no-op once zero is reached (so extra calls are harmless, which matters because callers often cannot prove exactly-once). await() blocks until the count reaches zero, and returns IMMEDIATELY and forever thereafter — a thread that calls await() an hour after the latch opened must not block. await(timeout) returns a boolean rather than throwing, so the caller can distinguish 'gate opened' from 'gave up'. getCount() is a diagnostic, explicitly not a control primitive, because any code that reads the count and then acts on it is racing. The latch is ONE-SHOT by contract: there is no reset(), and refusing to add one is a design decision worth defending — a resettable latch has no safe semantics for a thread already inside await(). CYCLICBARRIER: constructed with a party count N and an optional barrierAction Runnable. await() blocks until N threads have arrived; the Nth arrival runs the barrierAction (on its own thread, with all others still blocked), then TRIPS the barrier, releasing all N and resetting the count for the next round. await() returns the caller's arrival index (N-1 for the first arriver down to 0 for the last) so callers can elect a leader without extra coordination. The barrier must be reusable for an unbounded number of rounds. Cross-cutting non-functional requirements: both must be INTERRUPT-RESPONSIVE (a blocked thread must observe Thread.interrupt promptly rather than hanging until shutdown); both must not busy-wait; and the barrier needs a defined failure mode — if one participant is interrupted or times out while others wait, the remaining threads must NOT deadlock forever, which is the broken-barrier requirement. Memory visibility is a requirement, not an implementation detail: actions performed before countDown() must be visible to a thread that returns from await()."),
      C("entities", "Core entities & responsibilities",
        "COUNTDOWNLATCH is almost trivially small and that is the point: an int count and a monitor. Its methods are countDown(), await(), await(timeout, unit), and getCount(). It records NO owner and no waiter list — anyone may count down, anyone may await, and there is no notion of a thread 'holding' the latch, which distinguishes it sharply from a lock and is why it composes so freely across unrelated subsystems. CYCLICBARRIER is the interesting class and has four fields, each earning its place. parties is the immutable N. count is the number of arrivals still outstanding IN THE CURRENT ROUND, reset to parties on each trip. GENERATION is the crucial one: a plain Object (or a small class with a broken flag) whose IDENTITY represents the current round; tripping the barrier replaces it with a fresh instance, and every waiter compares the generation it captured on arrival with the current one. barrierAction is the optional Runnable executed by the last arriving thread, before any waiter is released — this ordering is the contract that makes 'merge the eight partial results, then let everyone continue' safe with no extra synchronization. THE MONITOR (in a hand-rolled version, the object's intrinsic lock; in the java.util.concurrent version, a ReentrantLock plus a Condition) guards every field and provides the wait-set. BROKENBARRIEREXCEPTION is a real entity in the design, not an afterthought: it is the mechanism by which one participant's failure is broadcast to all the others so nobody waits forever. When comparing to the JDK, the entity worth naming is ABSTRACTQUEUEDSYNCHRONIZER — CountDownLatch is implemented as an AQS with the count as the shared state and tryAcquireShared returning success once state hits zero, which is why release wakes all shared waiters at once rather than one at a time."),
      C("design", "Class design: guarded waits, notifyAll, and where the two designs diverge",
        "Both classes are built from the same monitor discipline, and stating that discipline precisely is most of the interview. RULE ONE: every wait is inside a WHILE loop testing a predicate, never an if. A returning wait() means only 'something changed, re-check' — it does not mean the predicate holds. Two reasons, both concrete: spurious wakeups are permitted by the JLS and do occur, and more importantly a woken thread must reacquire the monitor, during which another thread can run and invalidate the condition. RULE TWO: use notifyAll(), never notify(). For a latch this is definitional — reaching zero must release ALL waiters, and notify() would release exactly one and leave the rest blocked forever, a deadlock that only appears when more than one thread waits. For a barrier the same holds on a trip. RULE THREE: mutate the guarded state and signal while HOLDING the monitor, so no waiter can slip between the state change and the signal and miss it. Now the divergence. The latch's predicate is simply count > 0, and because the state is monotonic — it only ever decreases and never resets — that predicate is safe forever; a thread that arrives after the open never waits at all, which is the required late-arrival behaviour and comes for free from the while loop. The barrier CANNOT use its count as the predicate, because count is reset to parties the instant the barrier trips: a thread that is woken by the trip, then reacquires the lock after the count has already been reset, would re-evaluate count > 0 as TRUE and go back to waiting — losing an entire round, or hanging permanently. The barrier's predicate must therefore be an identity check against the generation captured on arrival. Patterns, honestly labelled: this is the MONITOR OBJECT pattern (condition synchronization co-located with the state it guards), with the barrierAction as a small TEMPLATE METHOD hook. What is deliberately absent is any spin, any sleep-poll, and any attempt to be clever with a single-permit notify — all three are the classic wrong answers."),
      K("code", "Core classes (Java)",
        "// ── ONE-SHOT: counts to zero and stays open. ────────────────────────────\nfinal class CountDownLatch {\n  private int count;\n  CountDownLatch(int count) {\n    if (count < 0) throw new IllegalArgumentException(\"count < 0\");\n    this.count = count;\n  }\n\n  synchronized void countDown() {\n    if (count == 0) return;              // idempotent past zero — extra calls are harmless\n    if (--count == 0) notifyAll();       // ALL waiters must be released, not one\n  }\n\n  synchronized void await() throws InterruptedException {\n    while (count > 0) wait();            // WHILE, not if: a wakeup is a hint, not a promise\n  }                                      // once open it never closes, so late arrivals sail through\n\n  synchronized boolean await(long timeoutMs) throws InterruptedException {\n    long deadline = System.nanoTime() + timeoutMs * 1_000_000L;\n    while (count > 0) {\n      long remainMs = (deadline - System.nanoTime()) / 1_000_000L;\n      if (remainMs <= 0) return false;   // recompute the remaining wait EVERY iteration\n      wait(remainMs);\n    }\n    return true;\n  }\n\n  synchronized long getCount() { return count; }   // diagnostic only — acting on it is a race\n}\n\n// ── REUSABLE: resets after every round, so it needs GENERATIONS. ───────────\nfinal class CyclicBarrier {\n  private static final class Generation { boolean broken = false; }\n\n  private final int parties;\n  private final Runnable barrierAction;\n  private int count;\n  private Generation generation = new Generation();\n\n  CyclicBarrier(int parties, Runnable barrierAction) {\n    if (parties <= 0) throw new IllegalArgumentException(\"parties <= 0\");\n    this.parties = parties; this.count = parties; this.barrierAction = barrierAction;\n  }\n\n  /** @return this thread's arrival index: parties-1 for the first arriver, 0 for the last. */\n  synchronized int await() throws InterruptedException, BrokenBarrierException {\n    final Generation myGen = generation;               // capture MY round\n    if (myGen.broken) throw new BrokenBarrierException();\n\n    int index = --count;\n    if (index == 0) {                                  // I am the LAST to arrive\n      boolean ranAction = false;\n      try {\n        if (barrierAction != null) barrierAction.run();  // runs on MY thread, others still blocked\n        ranAction = true;\n        nextGeneration();                              // trip: new generation + notifyAll + reset\n        return 0;\n      } finally {\n        if (!ranAction) breakBarrier();                // action threw — do not strand the others\n      }\n    }\n\n    while (true) {\n      try {\n        wait();\n      } catch (InterruptedException e) {\n        if (generation == myGen && !myGen.broken) { breakBarrier(); throw e; }\n        Thread.currentThread().interrupt();            // already tripped: preserve the flag, proceed\n      }\n      if (myGen.broken) throw new BrokenBarrierException();\n      if (generation != myGen) return index;           // THE PREDICATE: my round is over\n      // else: spurious wakeup, or a signal for a round that is not mine — keep waiting\n    }\n  }\n\n  private void nextGeneration() {\n    notifyAll();                                       // release everyone waiting on the old gen\n    count = parties;\n    generation = new Generation();                     // identity change IS the release signal\n  }\n\n  private void breakBarrier() {\n    generation.broken = true;\n    count = parties;\n    notifyAll();                                       // everyone wakes and throws — nobody hangs\n  }\n\n  synchronized boolean isBroken() { return generation.broken; }\n\n  synchronized void reset() { breakBarrier(); generation = new Generation(); count = parties; }\n}"),
      C("deep_dive", "Deep dive: why a reusable barrier needs generations",
        "This is the mechanism that carries the topic, and it is worth walking the failing interleaving out loud because that is exactly what the interviewer wants to hear. Suppose the barrier has three parties and the (wrong) implementation waits on the predicate count > 0. Threads A and B arrive for round 1 and block. C arrives, decrements count to zero, resets count to 3 for the next round, and calls notifyAll(). Now A and B are RUNNABLE but neither holds the monitor — they must reacquire it before wait() can return, and reacquisition is not instantaneous. Meanwhile C leaves await(), finishes its round-2 work quickly (imagine C's slice of the simulation is small), and calls await() again for round 2, decrementing count to 2. A finally reacquires the lock, returns from wait(), and re-tests its predicate: count is 2, which is greater than zero, so A calls wait() AGAIN. A has just gone back to sleep despite its round having completed. If B does the same, all three threads are now waiting for a round-2 trip that requires three arrivals but has already consumed one — the simulation hangs. Worse variants leak in the other direction: with different timing, a thread from round 2 can be released by round 1's notifyAll and run ahead of the others, producing silent data corruption rather than a hang. Note the shape of this bug — it needs a specific interleaving, so it passes ten thousand test runs and fails in production on a machine with a different core count. The generation fix is exact: the barrier's state includes an object whose IDENTITY names the current round; each waiter captures it on arrival; tripping installs a brand-new object; and the wait predicate becomes 'generation != myGen'. That predicate is monotonic FOR A GIVEN WAITER — once your round is over, it is over forever, and no subsequent round can push you back into waiting, because a new generation is never equal to your old one. That property is what makes the while loop safe again, and it is the same trick used in the JDK's CyclicBarrier and in sequence-number-based condition variables generally. A candidate who says 'you need a generation because the count is reused and therefore is not a valid predicate' has answered the hardest part of this question in one sentence."),
      C("deep_dive", "Deep dive: guarded waits, notifyAll vs notify, and lost wakeups",
        "Three monitor rules do the rest of the work, and each has a concrete failure attached. WHILE, NOT IF. Writing 'if (count > 0) wait();' compiles, passes casual tests, and is wrong for two independent reasons. The JLS explicitly permits SPURIOUS WAKEUPS — wait() may return without any notify — because it is implemented on OS primitives that allow it, so the predicate must be re-verified. And even without spurious wakeups, wait() returns by REACQUIRING THE MONITOR, and between the notifyAll and that reacquisition an arbitrary amount of other code can run, including code that invalidates the condition (a barging thread consuming the permit, or, here, the barrier count resetting). The while loop is not defensive style, it is the contract. NOTIFYALL, NOT NOTIFY. notify() wakes ONE arbitrary waiter from the wait-set. For a latch this is straightforwardly fatal: hitting zero must release every waiter, and notify() strands all but one, producing a deadlock that only shows up when more than one thread waits — which is often not the case in the unit test. The general rule is that notify() is only safe when all waiters are HOMOGENEOUS (every waiter is waiting for the same condition and any one of them can consume the signal) AND the signal is a consumable resource. A latch's open state is not consumable, so notifyAll is required. Even in cases where notify() would be technically sufficient, notifyAll costs a thundering-herd wakeup of N threads that immediately re-check a predicate and mostly go back to sleep — a real cost at high N, and the honest answer is that you accept it for correctness or move to explicit Condition objects (one Condition per distinct predicate, with signalAll on the right one) if profiling shows it matters. SIGNAL WHILE HOLDING THE LOCK. The mutation and the notifyAll must both happen inside the synchronized region. If you release the lock, then notify, a waiter can enter the wait-set between the two and MISS the signal — the classic lost-wakeup — and since a latch signals only once, that waiter blocks forever. This is also why the check-then-wait sequence must be atomic: testing count > 0 and calling wait() are two operations, and they are safe only because the monitor is held across both and wait() releases it ATOMICALLY as it parks. Finally, MEMORY VISIBILITY: the monitor gives happens-before, so everything a worker did before countDown() is visible to a thread returning from await(). Without it, the gate could open while the data it gates is still in another core's store buffer."),
      C("deep_dive", "Deep dive: broken barriers, interrupts, and timeouts",
        "A reusable barrier introduces a failure mode a latch does not have: an ALL-OR-NOTHING rendezvous means one participant's failure must not leave the others waiting for an arrival that will never come. Consider eight simulation workers; worker 5 is interrupted by a shutdown signal while blocked in await(). If it simply throws InterruptedException and leaves, the barrier's count is permanently one short — the remaining seven wait forever for an eighth arrival, and the process hangs on shutdown, which is one of the more embarrassing production bugs because it only manifests during an incident. The correct design is the BROKEN BARRIER state: the exiting thread sets generation.broken = true, restores count to parties, and calls notifyAll(); every other waiter wakes, sees the broken flag, and throws BrokenBarrierException. Now all eight fail fast and the caller decides — abort the run, or call reset() and start a fresh generation. Note the deliberate asymmetry: the thread that was interrupted throws InterruptedException (its own thread was targeted), while the collateral victims throw BrokenBarrierException (they were not interrupted, the rendezvous failed). Getting that distinction right is a genuine senior signal. TIMED AWAIT propagates identically — a thread that times out must break the barrier before returning, because a rendezvous missing one party is unusable. There is also a subtle interrupt case in the code above: if a thread is interrupted AFTER the barrier has already tripped for its generation, throwing would be wrong (its round genuinely completed and its work is committed), so the implementation re-asserts the interrupt flag with Thread.currentThread().interrupt() and returns normally, letting the caller observe the interrupt at the next cancellation point. Swallowing an interrupt without re-asserting it is the standard bug — the thread silently becomes uncancellable. THE BARRIERACTION deserves the same care. It runs on the last arriving thread while everyone else is still blocked, which is precisely the guarantee that lets it merge partial results with no locking; but if it THROWS, the barrier must be broken rather than tripped, or the other seven threads wait forever on a trip that never happened — hence the try/finally in await(). Compare CyclicBarrier with the JDK's PHASER, the modern generalization: it supports DYNAMIC registration and deregistration of parties, which the fixed-parties barrier cannot, and it handles the common case where a worker finishes early and must leave the rendezvous without breaking it. Naming Phaser as the answer to 'what if the number of workers changes between rounds' closes the topic properly."),
      C("tradeoffs", "Trade-offs & extension points",
        "LATCH vs BARRIER: the decision rule is one question — does the gate reopen? A latch is one-shot and ASYMMETRIC (the threads that count down are usually not the threads that wait: initializers signal, the server waits). A barrier is cyclic and SYMMETRIC (the participants are the waiters). Using a latch where you need a barrier means constructing a new latch every round, which sounds acceptable until you notice the publication problem: every worker must see the NEW latch reference, so you need another synchronizer to hand it out — you have just reinvented generations, worse. Using a barrier where you need a latch fails because barriers require exactly N arrivals and cannot express 'open once, and everyone after that passes freely'. INTRINSIC MONITOR vs REENTRANTLOCK + CONDITION: the synchronized version is shorter and needs no cleanup, but it has ONE wait-set, so every signal wakes every waiter. A ReentrantLock gives multiple Conditions (one per predicate) and adds tryLock, lockInterruptibly, timed acquisition, and optional FAIRNESS. For these two synchronizers, one wait-set is genuinely fine because all waiters share one predicate. BUILDING ON AQS: the real CountDownLatch uses AbstractQueuedSynchronizer with the count as shared state, giving an ordered wait queue, timed and interruptible acquisition, and shared-mode release for free — the right answer to 'how would you productionize this' is 'extend AQS rather than hand-roll a monitor'. SEMAPHORE vs LATCH: a Semaphore(0) released N times can imitate a latch, but permits are CONSUMABLE, so the eleventh waiter on a ten-permit semaphore blocks whereas a latch is open to everyone forever — a subtle substitution bug worth naming. BARRIER vs PHASER: Phaser adds dynamic party registration, tiering for scale, and non-blocking arrival (arrive() without awaiting); prefer it when the participant count varies. FORK/JOIN or CompletableFuture.allOf is the modern answer when the requirement is really 'wait for N TASKS' rather than 'wait for N THREADS' — the latch is a thread-level primitive and using it to join tasks wastes a blocked thread per task. WHAT BREAKS AT SCALE: notifyAll thundering herds at high party counts; a barrier is only as fast as its SLOWEST participant every single round, so a barrier-per-timestep simulation with unbalanced work spends most of its time idle (the fix is work-stealing or fewer, coarser barriers); and lazily cancelled participants require the Phaser model. EXTENSIONS: a countUp/resettable latch is deliberately NOT offered because it has no safe semantics for threads already inside await(); if you truly need it, model it as a Phaser or hand out a fresh latch through a volatile reference published under the same lock."),
    ],
    "This pair is the standard probe for whether a candidate understands MONITORS rather than just calling library classes. Interviewers ask for a latch first — it is small enough to write on a whiteboard — and grade three specifics: while instead of if (with the spurious-wakeup and reacquisition reasons), notifyAll instead of notify (with the concrete deadlock that notify causes), and mutate-then-signal inside the lock (with the lost-wakeup that follows from releasing early). Then comes the real question: make it reusable. That is where most candidates fail, because they keep the count as the wait predicate and cannot explain why a fast thread from the next round breaks it. The expected answer is generation tracking, and being able to narrate the exact interleaving — C trips, resets the count, laps into round 2, and A wakes to find count > 0 and sleeps again — is what separates a hire from a lean-no-hire. The follow-ups are broken-barrier semantics on interrupt or a throwing barrier action, the difference between InterruptedException for the victim and BrokenBarrierException for the collateral waiters, and finally which JDK primitive you would actually reach for in production: AQS-backed CountDownLatch, CyclicBarrier, Phaser for dynamic parties, or CompletableFuture.allOf when the units are tasks rather than threads.",
    [
      G("count_as_barrier_predicate", "Reusing the arrival count as the wait predicate in a cyclic barrier, so a thread woken by the trip re-tests count > 0 after the reset and goes back to sleep — losing a round or hanging.", "Track a GENERATION object whose identity names the round; each waiter captures it on arrival and waits while generation == myGen. Tripping installs a fresh generation, which is monotonic per waiter and therefore a safe predicate.", "A barrier with only parties and count fields and no generation; simulations that hang after thousands of rounds; a bug that reproduces on a 16-core machine but never on a laptop."),
      G("notify_instead_of_notifyall", "Calling notify() when the latch reaches zero, releasing exactly one waiter and leaving every other blocked thread parked forever.", "Use notifyAll(): opening a gate is not a consumable resource handed to one thread, it is a state change all waiters must observe. Reserve notify() for homogeneous waiters competing for a consumable permit.", "notify() inside countDown() or a barrier trip; a startup gate that works with one waiter in the unit test and deadlocks with three in production."),
      G("interrupt_strands_the_others", "Letting an interrupted or timed-out participant throw and leave await() without touching shared state, so the remaining parties wait forever for an arrival that will never come.", "Implement broken-barrier semantics: the departing thread sets broken, restores count, and notifyAll()s; the others wake and throw BrokenBarrierException. Wrap the barrierAction in try/finally so a throwing action breaks rather than strands. Re-assert the interrupt flag when the round had already tripped.", "catch (InterruptedException e) { return; } inside await(); shutdown that hangs with N-1 threads parked in Object.wait; a swallowed interrupt with no Thread.currentThread().interrupt()."),
    ],
    0.6, DIAG.latchbarrier, "CountDownLatch / CyclicBarrier class diagram"),

  // ─────────────────── LOCK-FREE STACK ──────────────────────────────────────
  TT("lld_m20", "lld_m20_t5", 5, "Design a Lock-Free Stack", "design-lock-free-stack",
    ["case-study", "concurrency", "lock-free", "cas"],
    "A profiler shows 60% of your allocator's time inside lock acquisition on a free-list stack pushed and popped by 32 threads. Nobody holds the lock long — the critical section is three instructions — but every acquisition is a trip through the kernel when contended, and one thread descheduled inside the critical section blocks all 31 others. Someone suggests removing the lock entirely. Design that stack, then answer the question that decides whether you actually understand it: describe a concrete interleaving in which your compare-and-swap succeeds and yet the stack ends up corrupted.",
    "The whole structure reduces to one mutable word — the head pointer — and one instruction: compare-and-swap. Read the head, build your intended new head, and atomically install it ONLY IF the head is still what you read; if not, discard your work and retry. That gives a stack with no lock, no blocking, and no possibility that a descheduled thread stalls anyone else. The trap is that CAS compares a POINTER, not a HISTORY: it cannot tell 'unchanged' from 'changed and changed back'. That is the ABA problem, and on a stack it is not theoretical — a resurrected head node carries a stale next pointer that silently resurrects popped nodes or drops live ones. The fixes are versioned pointers or a reclamation scheme, and knowing that Java's GC removes the use-after-free half of the problem but NOT ABA itself is the line between having read about lock-free code and having written it.",
    [
      C("requirements", "Requirements",
        "Functional: a LIFO collection supporting push(E) and pop() returning null (or an Optional) when empty, plus isEmpty() as a best-effort diagnostic. The concurrency requirement is the real specification and must be stated in the precise vocabulary the interviewer is listening for. LINEARIZABILITY: every push and pop must appear to take effect atomically at some instant between its invocation and its return, so that any observed history is explainable by some sequential order consistent with real time. LOCK-FREEDOM: at least one thread makes progress in a bounded number of steps regardless of what the others do — including if one of them is descheduled, page-faulted, or killed mid-operation. That last clause is the actual motivation and is worth saying explicitly: a mutex-based stack is not merely slower under contention, it is FRAGILE, because the OS can preempt a thread holding the lock and every other thread then waits for the scheduler. Note carefully what is NOT promised: lock-freedom is not WAIT-freedom. An individual thread can, in principle, retry forever while others keep succeeding; there is no per-thread bound, and promising one is a factual error the interviewer will catch. Non-functional: no allocation on the pop path beyond what the JVM already does; predictable behaviour under high contention, including a defined back-off strategy; and correct MEMORY VISIBILITY — a value pushed by one thread must be fully visible to the thread that pops it, which requires the volatile/atomic semantics of the head write rather than a plain store. Explicitly out of scope: bounded capacity (a lock-free bounded stack needs a separate size CAS and reintroduces a contention point), blocking take() semantics (that is a different data structure — a lock-free stack cannot block by construction, so blocking must be layered above), and iteration, which cannot be made meaningfully consistent without a snapshot mechanism."),
      C("entities", "Core entities & responsibilities",
        "The entity list is deliberately tiny, and that minimalism is itself the design argument: every additional mutable field is another thing that must be updated atomically, and CAS updates exactly ONE word. TREIBERSTACK<E> is the whole public surface — push, pop, isEmpty — and owns exactly one field. HEAD is an AtomicReference<Node<E>> (or, in modern Java, a VarHandle over a volatile field, which avoids the extra AtomicReference object). It is THE single point of mutation and therefore the single point of contention; the entire correctness argument is 'the stack's state is a function of head, and head only ever changes by a successful CAS'. NODE<E> holds a value and a next reference. The critical design property is that a node's next is written ONCE, BEFORE the node is published via the successful CAS, and never mutated afterwards — publishing an immutable node through an atomic write is what gives readers a consistent chain with no additional synchronization; a design that mutates next after publication has to reason about torn chains and is almost certainly wrong. THE RETRY LOOP is not a class but is the load-bearing control structure and should be named as such in the design conversation: read a consistent local snapshot, compute, attempt, and on failure re-read from scratch — never reuse a stale local. ATOMICSTAMPEDREFERENCE<Node<E>> is the entity that appears when ABA must be defeated: it pairs the pointer with a monotonically increasing stamp so CAS compares (pointer, version) instead of pointer alone. BACKOFF is an optional collaborator — an exponential randomized pause, or Thread.onSpinWait() — that exists purely to reduce cache-line ping-pong under heavy contention. For the classic exam follow-up, ELIMINATIONARRAY is the collaborator that lets a concurrent push and pop cancel each other out without ever touching head, converting the structure's central bottleneck into a parallel one."),
      C("design", "Class design: one word, one CAS, and a retry loop",
        "The design is a single invariant plus the mechanics that preserve it: the stack IS the chain hanging off head, and head changes only through a successful compare-and-swap. PUSH: allocate a node holding the value; loop — read h = head.get(); set node.next = h (a plain write to a node no other thread can see yet); attempt head.compareAndSet(h, node); if it fails, another thread changed head between the read and the attempt, so loop and re-read. POP: loop — read h = head.get(); if h is null return null (empty); read next = h.next; attempt head.compareAndSet(h, next); on success return h.value. Three details in that sketch are where candidates lose points. First, the FAILURE PATH MUST RE-READ EVERYTHING. Recomputing from a cached local is the classic bug — after a failed CAS your snapshot is provably stale, so every value derived from it is suspect. Second, publication ORDER on push: next is assigned before the CAS, and the CAS is a volatile write, so the fully-initialized node becomes visible atomically. A thread that observes the new head is guaranteed to observe the node's fields, because the CAS provides the release and the reader's volatile read of head provides the acquire — this is the happens-before edge that makes the structure work without any lock. Third, an EMPTY CHECK inside pop is part of the retry loop, not before it: the stack can empty between the read and the CAS. Patterns, named honestly: this is OPTIMISTIC CONCURRENCY CONTROL — assume no conflict, detect it at commit time, retry on detection — the same shape as a database's compare-and-set on a version column, and saying so places the design in a family rather than treating it as a trick. What is deliberately ABSENT: no lock anywhere, no size counter (maintaining an accurate size would require a second atomic and destroy linearizability of the pair unless CASed together, so size is either omitted or explicitly documented as approximate), and no Node reuse pool — because reusing nodes is precisely what turns the theoretical ABA problem into a live one on a garbage-collected runtime."),
      K("code", "Core classes (Java)",
        "// ── Treiber stack: one CAS on head, plus a retry loop. ─────────────────\nfinal class TreiberStack<E> {\n  private static final class Node<E> {\n    final E value; Node<E> next;                    // next is written BEFORE publication\n    Node(E value) { this.value = value; }\n  }\n\n  private final AtomicReference<Node<E>> head = new AtomicReference<>();\n\n  void push(E value) {\n    Node<E> node = new Node<>(value);               // fresh node — nobody else can see it yet\n    while (true) {\n      Node<E> h = head.get();                       // 1. snapshot\n      node.next = h;                                // 2. link (still private)\n      if (head.compareAndSet(h, node)) return;      // 3. publish atomically, or retry\n      // CAS failed => head moved => the snapshot is stale => re-read from scratch\n    }\n  }\n\n  E pop() {\n    while (true) {\n      Node<E> h = head.get();\n      if (h == null) return null;                   // empty check lives INSIDE the loop\n      Node<E> next = h.next;\n      if (head.compareAndSet(h, next)) {\n        h.next = null;                              // help GC; h is unreachable to others now\n        return h.value;\n      }\n    }\n  }\n\n  boolean isEmpty() { return head.get() == null; }  // best effort — stale the instant it returns\n}\n\n// ── ABA-hardened variant: CAS on (pointer, version), not pointer alone. ──\nfinal class StampedStack<E> {\n  private static final class Node<E> { final E value; Node<E> next; Node(E v) { value = v; } }\n\n  private final AtomicStampedReference<Node<E>> head =\n      new AtomicStampedReference<>(null, 0);\n\n  void push(E value) {\n    Node<E> node = new Node<>(value);\n    int[] stampHolder = new int[1];\n    while (true) {\n      Node<E> h = head.get(stampHolder);            // read pointer AND version together\n      int stamp = stampHolder[0];\n      node.next = h;\n      if (head.compareAndSet(h, node, stamp, stamp + 1)) return;\n    }\n  }\n\n  E pop() {\n    int[] stampHolder = new int[1];\n    while (true) {\n      Node<E> h = head.get(stampHolder);\n      if (h == null) return null;\n      int stamp = stampHolder[0];\n      Node<E> next = h.next;\n      // Even if the pointer came back as A, a DIFFERENT stamp fails the CAS — ABA defeated.\n      if (head.compareAndSet(h, next, stamp, stamp + 1)) { h.next = null; return h.value; }\n    }\n  }\n}\n\n// ── Contention relief: back off instead of hammering the cache line. ─────\nfinal class BackoffStack<E> {\n  private final AtomicReference<TreiberStack<E>> unused = null;   // illustrative only\n  private final AtomicReference<Object> head = new AtomicReference<>();\n\n  static void backoff(int attempt) {\n    if (attempt < 4) { Thread.onSpinWait(); return; }             // brief spin — the common case\n    int capped = Math.min(attempt, 10);\n    long nanos = ThreadLocalRandom.current().nextLong(1L << capped);  // exponential + randomized\n    LockSupport.parkNanos(nanos);                                 // randomized to break lockstep\n  }\n}"),
      C("deep_dive", "Deep dive: the ABA problem, concretely",
        "This is the mechanism the topic exists to teach, so learn a specific interleaving rather than a definition. The stack holds A -> B -> C, with head = A. Thread T1 calls pop(): it reads h = A, reads next = A.next = B, and is then DESCHEDULED — by the OS, a page fault, a GC safepoint, anything — after the read and before the CAS. While T1 is stopped, thread T2 runs: it pops A (head becomes B), pops B (head becomes C), and then pushes A back — perhaps A came from an object pool, or the pushed value simply happens to be the same node object. The stack is now A -> C, and head = A again. T1 resumes and executes head.compareAndSet(A, B). The comparison SUCCEEDS, because head really is A — the pointer is bit-for-bit what T1 read. But B was popped and is no longer in the stack, so T1 has just installed a stale successor: head becomes B, and the stack is now B -> C with A silently lost and B resurrected. Nothing threw, nothing warned; the data structure is simply corrupt, and the damage surfaces later as duplicate elements, lost elements, or a cycle. The root cause stated precisely: CAS answers 'is this word still equal to what I read', but the algorithm needs the strictly stronger 'has this word been UNMODIFIED since I read it'. Equality cannot distinguish 'unchanged' from 'changed and restored'. Fixes, in order of practicality. (1) VERSIONED/TAGGED POINTERS: bundle a monotonic counter with the pointer and CAS the pair — AtomicStampedReference in Java, or a double-width CAS packing a tag into unused pointer bits in C++. Every successful modification bumps the version, so a restored pointer carries a different stamp and the stale CAS fails as it should. This is the standard answer. (2) HAZARD POINTERS: each thread publishes the nodes it is currently dereferencing; a would-be reclaimer must not free a node any thread has flagged. (3) EPOCH-BASED RECLAMATION / RCU: defer reuse until every thread has passed a quiescent point. Now the Java-specific nuance that earns real credit: a garbage collector removes the USE-AFTER-FREE half of the problem — node A cannot be freed and reallocated as a different object while T1 still holds a reference — which is why textbook Treiber stacks in Java usually get away without stamps. But GC does NOT remove ABA. If the same node OBJECT is pushed again (object pools, node reuse for allocation avoidance, or a caller pushing the same instance twice), the pointer genuinely returns to A and the interleaving above plays out exactly. So the correct answer to 'do I need AtomicStampedReference in Java' is conditional: not if every pushed node is freshly allocated and never recycled; yes if nodes are pooled or reused — and stating that condition, rather than reciting 'Java has GC so ABA cannot happen', is the senior answer."),
      C("deep_dive", "Deep dive: progress guarantees — lock-free is not wait-free",
        "The vocabulary here is precise, interviewers use it precisely, and misusing it reads as bluffing. BLOCKING: a thread that stops while holding the lock stops everyone. Note that the failure is not about performance — it is that progress becomes dependent on the SCHEDULER, so a preempted lock-holder, a page fault, or a thread killed inside the critical section halts the whole structure, potentially forever. OBSTRUCTION-FREE: a thread makes progress if it eventually runs in isolation (no contention) — the weakest useful guarantee. LOCK-FREE: at least ONE thread completes an operation in a bounded number of steps, system-wide, regardless of delays or failures of others. This is what the Treiber stack provides, and the proof is direct: a CAS on head fails only when some other thread's CAS succeeded, and every successful CAS is a completed operation — so failure of my attempt is evidence of someone else's progress, and the system as a whole cannot stall. WAIT-FREE: EVERY thread completes in a bounded number of its own steps, with no dependence on others. The Treiber stack is emphatically NOT wait-free: an unlucky thread can lose the CAS race indefinitely while others keep winning, so individual starvation is permitted by the contract. Claiming otherwise is a factual error and a common one. Practically, unbounded starvation is rare because CAS failure implies someone else succeeded and the winner then leaves, but 'rare' is not 'bounded' — and if a bound is genuinely required (hard real time), the answer is a wait-free algorithm with helping, where a thread that observes another's announced operation COMPLETES it on that thread's behalf; that machinery costs far more in the common case, which is why almost nobody ships it. The performance consequence deserves equal honesty, because it is the follow-up. Every thread CASing the same head pointer is hammering ONE cache line. Under heavy contention that line ping-pongs between cores, each transfer is dozens to hundreds of nanoseconds, and the failure rate climbs superlinearly with thread count — so a lock-free stack can be measurably SLOWER than a well-implemented lock, whose queued waiters at least park quietly instead of burning bus bandwidth. The mitigations are, in order: exponential randomized BACKOFF after failed attempts (randomization matters — without it, threads retry in lockstep and collide again); Thread.onSpinWait() to hint the CPU during the brief spin; and, structurally, an ELIMINATION ARRAY, where a pushing thread and a popping thread that both fail their CAS meet in a random slot and exchange values directly, satisfying both operations without touching head at all. Elimination is beautiful precisely because it converts the contention bottleneck into extra parallelism — under high load the stack gets FASTER — and it is linearizable because a matched push/pop pair can be ordered adjacently in the history."),
      C("deep_dive", "Deep dive: memory ordering, publication, and reclamation",
        "Lock-free code fails on MEMORY VISIBILITY as often as it fails on logic, and the reasoning is invisible in the source. In push, node.next = h is a plain (non-volatile) write to an object no other thread can reach yet, and then head.compareAndSet publishes the node. That ordering is not stylistic. The CAS on an AtomicReference has volatile-write (release) semantics, so under the Java Memory Model everything the writing thread did BEFORE the CAS — including initializing value and next — happens-before any thread's subsequent volatile READ of head. A popping thread reads head, sees the new node, and is therefore guaranteed to see fully initialized fields. Reverse the order — publish the node and then set next — and a reader can observe a node whose next is still null, truncating the stack. This is the same hazard as unsafe double-checked-locking publication, and naming that connection lands well. It is also why final fields help: a final field is guaranteed visible after construction without additional synchronization, so making value final (and next final in an immutable-node design) removes a whole class of races. The same argument explains why isEmpty() is only best-effort: it is a single volatile read, correct at the instant it executes and potentially wrong by the time the caller branches on it — so any check-then-act built on it is a race, and the API should document that. RECLAMATION is the other half of the story, and it is where C++ and Java diverge sharply. In a manually managed language the popping thread cannot simply free the popped node: another thread may be in the middle of pop() holding a pointer to it, having read h but not yet CASed — freeing underneath that thread is a use-after-free, and this is precisely the problem hazard pointers and epoch-based reclamation exist to solve. In Java the GC handles it: a node stays alive exactly as long as any thread holds a reference, so the memory-safety problem evaporates and the Treiber stack is genuinely ten lines. Two Java-specific residues remain. First, setting h.next = null after a successful pop is a real MEMORY-LEAK guard, not decoration: leaving the popped node linked to the rest of the stack can keep a long chain reachable through a stale reference held by a caller — the same class of bug as the famous ArrayDeque and ThreadLocal leaks. Second, allocation per push means GC pressure, and this is exactly the pressure that tempts teams into node pooling — which, as the ABA section showed, is the thing that reintroduces ABA. That circle is worth stating explicitly, because it shows the trade-offs in this design are coupled rather than independent."),
      C("tradeoffs", "Trade-offs & extension points",
        "LOCK-FREE STACK vs SYNCHRONIZED STACK: the lock version is shorter, obviously correct, trivially extended to compound operations (popTwo, drain), and under LOW contention is roughly as fast — an uncontended lock in Java is a biased or thin lock costing a handful of nanoseconds. The lock-free version wins on ROBUSTNESS (no thread can stall the structure by being descheduled), on latency predictability (no kernel transitions, no convoying), and in signal-handler or GC-critical contexts where blocking is forbidden. Under HIGH contention neither obviously wins: the lock convoys, the CAS storm saturates a cache line, and the honest answer is 'measure with your thread count and access pattern'. Say that rather than declaring lock-free universally faster, which is the trap. STACK vs QUEUE: a lock-free stack mutates one pointer; the Michael-Scott lock-free QUEUE must mutate head and tail and cannot do both in one CAS, so it needs the HELPING protocol where a thread that finds tail lagging advances it on the previous thread's behalf. That is a genuinely harder algorithm, and knowing WHY (two mutation points, one CAS) is the useful insight. Prefer the JDK's ConcurrentLinkedQueue over hand-rolling it. VERSIONED POINTER vs PLAIN REFERENCE: AtomicStampedReference costs an extra allocated pair object per update in the standard implementation and an int[] holder per read, so it is not free; use it only when nodes can be recycled. Alternatives: pack a tag into spare pointer bits (native code), or simply guarantee fresh allocation per push (the usual Java answer). ELIMINATION BACKOFF is the standard scale extension: pushes and pops that fail their CAS pair up in a random exchange slot, cancelling out without touching head, which turns the single hot pointer into parallel exchanges — throughput rises with contention rather than collapsing. Its cost is complexity and a small latency penalty for the rendezvous timeout when no partner shows up. LOCK-FREE vs WAIT-FREE: helping-based wait-free versions bound per-thread latency but are markedly slower in the common case; ship one only if a hard real-time bound is a stated requirement. WHAT BREAKS AT SCALE: the single hot cache line is the ceiling — beyond roughly the core count of one socket, expect degradation without elimination or striping; there is no bounded capacity, so an unbounded producer with a slow consumer becomes an OutOfMemoryError, and adding capacity means a second atomic and a much subtler design; and there is no blocking pop, so a consumer must spin or park with a condition layered above — at which point ask whether LinkedBlockingDeque or a Disruptor-style ring buffer is the better answer. EXTENSIONS worth naming: a lock-free size is deliberately omitted (an accurate count needs a second CAS and breaks linearizability of the pair), and a consistent iterator needs a snapshot, which for a stack is cheap — capture head once and walk the immutable chain, giving a genuinely consistent point-in-time view for free."),
    ],
    "The lock-free stack is the canonical interview vehicle for CAS reasoning, and it is graded almost entirely on the follow-ups rather than the ten lines of code. Interviewers expect you to write the retry loop cleanly (re-read the snapshot on failure, empty check inside the loop, link before publish), then they go straight for ABA: 'describe an interleaving where your CAS succeeds and the stack is corrupted'. A candidate who cannot narrate the read-A / stall / pop-A / pop-B / push-A sequence has not understood the primitive. The next probe is vocabulary: lock-free versus wait-free, and whether you claim a per-thread bound you do not have. Then memory ordering — why next is assigned before the CAS and what happens-before edge makes the node's fields visible to the popper. Then performance honesty: whether you know that a hot single cache line can make this slower than a lock, and that backoff and elimination are the mitigations. Strong candidates also draw the boundary at the queue: two mutation points cannot be CASed together, which is why Michael-Scott needs helping — and they close by saying they would use the JDK's implementation in production and hand-roll only with a measured reason.",
    [
      G("aba_dismissed_because_gc", "Asserting that ABA cannot happen in Java because the garbage collector prevents a node from being freed and reallocated.", "GC removes the use-after-free half only. If the same node OBJECT is pushed again — object pools, node reuse, or a caller pushing the same instance twice — the pointer genuinely returns to A and the stale CAS succeeds with a stale next. Use AtomicStampedReference whenever nodes can be recycled; fresh allocation per push is the alternative guarantee.", "A node pool or free list feeding push(); AtomicReference with node reuse; duplicated or vanished elements under load that never reproduce in single-threaded tests."),
      G("stale_snapshot_on_retry", "Retrying after a failed CAS without re-reading head — reusing the cached local h, or hoisting the head read outside the while loop.", "The failure of a CAS is proof that the snapshot is stale; re-read head and everything derived from it at the top of every iteration, and keep the empty check inside the loop since the stack can drain between the read and the attempt.", "Node h = head.get(); outside while(true); a pop that dereferences a next captured before the loop; NullPointerException on h.next under contention."),
      G("lockfree_claimed_waitfree", "Describing the Treiber stack as wait-free, or asserting that every thread is guaranteed to finish in bounded time because there is no lock.", "Lock-free guarantees only that SOME thread progresses system-wide — a CAS fails only because another succeeded. An individual thread may retry indefinitely. Wait-freedom requires a helping protocol and costs far more in the common case.", "Claims of starvation-freedom without a helping mechanism; assuming a lock-free structure is always faster than a lock; no backoff strategy despite high thread counts."),
    ],
    0.6, DIAG.lockfreestack, "Lock-free (Treiber) stack class diagram"),

  // ─────────────────── TOKEN BUCKET (CONCURRENCY PRIMITIVE) ─────────────────
  TT("lld_m20", "lld_m20_t6", 6, "Design a Token-Bucket Rate Limiter", "design-token-bucket",
    ["case-study", "concurrency", "atomics", "throttling"],
    "You need to cap an outbound integration at 100 calls per second. A junior engineer ships it in twenty lines: each bucket gets a ScheduledExecutorService task that adds a token every 10 ms, and tryAcquire does 'if (tokens > 0) { tokens--; return true; }'. It passes every test. In production with 40,000 API keys it schedules 40,000 repeating timer tasks, the scheduler thread saturates, buckets for keys nobody has called in three days keep ticking, the map grows without bound, and under load two threads occasionally both consume the same last token. Every one of those four failures has the same root: the design used a background thread where it needed arithmetic, and a read-modify-write where it needed one atomic step.",
    "A token bucket does not need to be TOLD that time passed — it can DERIVE it. Store two numbers, tokens and lastRefillNanos, and on every access compute tokens = min(capacity, tokens + (now - lastRefillNanos) * ratePerNano). No timer, no background thread, no work at all for an idle bucket: a bucket untouched for three days costs zero CPU and settles its entire debt in one multiply the moment someone calls it. That turns the per-bucket cost from a scheduled task into sixteen bytes of state. The second half is that refill-check-consume is a read-modify-write over that state, so it must be ONE atomic step — either a short synchronized block or, better, a CAS retry loop over an IMMUTABLE (tokens, lastRefillNanos) snapshot, so two callers can never both see the last token. And the clock must be monotonic: nanoTime, never currentTimeMillis, or one NTP step backwards freezes the limiter.",
    [
      C("requirements", "Requirements",
        "Functional: (1) tryAcquire(n) — non-blocking; consume n tokens and return true, or consume nothing and return false, with the all-or-nothing property being explicit (a partial consume that returns false is a silent leak). (2) acquire(n) — blocking; wait until n tokens are available, then consume them, with an interruptible variant and a timed tryAcquire(n, timeout) that returns false rather than throwing. (3) Configuration by refill RATE (tokens per second) and CAPACITY (the burst allowance), which are two independent numbers: capacity governs how much unused allowance a caller may bank, and setting capacity equal to rate means at most one second of burst. (4) Per-key limiting — the limiter is virtually never global; it is per API key, per user, per tenant, so a REGISTRY that maps key to bucket is part of the requirement, not an afterthought. Non-functional constraints, and these are the ones that actually drive the design. COST PER IDLE BUCKET MUST BE ZERO: with hundreds of thousands of keys of which a few hundred are active at any moment, any design with per-bucket background work is disqualified before it is written. THROUGHPUT: tryAcquire sits on the hot path of every request, so it must be lock-cheap and allocation-light — microseconds, not milliseconds. CORRECTNESS UNDER CONCURRENCY: with 200 threads calling the same bucket, the count of granted permits over any window must never exceed capacity + rate * window; no interleaving may over-issue. MEMORY: the key map must not grow without bound as keys churn, so eviction is a stated requirement. CLOCK ROBUSTNESS: the limiter must be immune to wall-clock adjustments. Explicitly out of scope here: comparing token bucket against sliding-window or fixed-window algorithms (a separate topic covers policy selection), and distributed enforcement across nodes, which is named only in the trade-offs."),
      C("entities", "Core entities & responsibilities",
        "TOKENBUCKET is the per-key limiter and owns exactly one piece of mutable state. STATE is the entity that makes the concurrent design tractable: an IMMUTABLE value object holding tokens (a double, so fractional accrual between calls is not lost to truncation) and lastRefillNanos (a long from the monotonic clock). Making it immutable is the load-bearing decision — the pair must move together, and if they are two separate mutable fields there is no way to update both atomically without a lock. AtomicReference<State> then gives a single-word CAS over the whole pair. Note the field choice: tokens as a double avoids the bug where a 100/sec bucket sampled every 5 ms computes 0.5 tokens, truncates to 0, and refills at zero forever — a real and common defect. REFILLPOLICY is arithmetic rather than a class: ratePerNano = rate / 1e9, precomputed once, and the derivation tokensNow = min(capacity, tokens + elapsed * ratePerNano). CLOCKSOURCE deserves to be a named seam (a LongSupplier defaulting to System::nanoTime), not for flexibility theatre but because a rate limiter is untestable without an injectable clock — you cannot unit-test a one-per-second limiter by sleeping. BUCKETREGISTRY owns the per-key lifecycle: a concurrent map from key to bucket, creation via computeIfAbsent so two concurrent first-requests for the same key cannot create two buckets, plus an EVICTION policy so keys that stop calling are reclaimed. STRIPING is the registry's contention answer when a plain map's per-bin locking is not enough — N shards, key.hashCode() selecting the shard, so unrelated keys never touch the same lock or the same cache line. WAITER is not a queue of parked threads: a blocking acquire computes the deficit, derives exactly how long that deficit takes to accrue, and sleeps that long — the absence of a waiter queue in the entity list is itself the design point."),
      C("design", "Class design: derived time, atomic consume, and no threads at all",
        "The design's spine is that the bucket contains no threads and no timers — it is a pure function of (previous state, now, requested permits). Every operation follows the same three steps fused into one atomic transition: refill from elapsed time, test the predicate, and consume. FUSED, not sequential — the classic bug is to refill under one atomic operation and then consume under another, at which point two threads can each refill, each observe one token, and each consume it. The concrete choices, each with its justification. (1) LAZY REFILL over a scheduled task: an idle bucket does zero work and the arithmetic is exact rather than quantized to a tick period, so a bucket does not have to wait for the next tick to grant a token that has already accrued. (2) IMMUTABLE STATE + CAS over synchronized: both are correct, and the honest framing is that synchronized is simpler and perfectly adequate at moderate contention (the critical section is a handful of instructions, so an uncontended intrinsic lock costs almost nothing), while the CAS loop avoids blocking entirely and degrades more gracefully when many threads share one hot key. Present both and pick with a reason — a candidate who says 'CAS is always better' has not measured one. (3) MONOTONIC CLOCK: System.nanoTime, never currentTimeMillis. (4) STORING lastRefillNanos rather than a token-add schedule keeps the state to two numbers, which is what makes the CAS possible at all. Patterns, honestly labelled: this is optimistic concurrency (read snapshot, compute, commit-if-unchanged), the same shape as a version-column update; the registry is a FLYWEIGHT-ish per-key cache; the clock is dependency injection for testability. What is deliberately ABSENT: no per-bucket thread, no timer wheel, no priority queue of waiters, no listener framework. The blocking path deliberately does NOT park threads on a condition — it computes the wait and sleeps — because a queue of waiters would require its own fairness machinery and a wakeup protocol, and that complexity is only worth paying when strict FIFO fairness is a requirement."),
      K("code", "Core classes (Java)",
        "/** Lazy-refill token bucket. No timers, no background threads, no locks. */\nfinal class TokenBucket {\n  /** Immutable so the PAIR can be swapped in one CAS. */\n  private static final class State {\n    final double tokens; final long lastRefillNanos;\n    State(double tokens, long lastRefillNanos) { this.tokens = tokens; this.lastRefillNanos = lastRefillNanos; }\n  }\n\n  private final double capacity;\n  private final double ratePerNano;          // tokens per nanosecond, precomputed\n  private final LongSupplier clock;          // injected: System::nanoTime — testable, monotonic\n  private final AtomicReference<State> state;\n\n  TokenBucket(double capacity, double tokensPerSecond, LongSupplier clock) {\n    if (capacity <= 0 || tokensPerSecond <= 0) throw new IllegalArgumentException();\n    this.capacity = capacity;\n    this.ratePerNano = tokensPerSecond / 1_000_000_000d;\n    this.clock = clock;\n    this.state = new AtomicReference<>(new State(capacity, clock.getAsLong()));  // start full\n  }\n\n  /** DERIVED refill — the whole point: elapsed time IS the token supply. */\n  private State refilled(State s, long now) {\n    long elapsed = now - s.lastRefillNanos;               // monotonic: never negative\n    if (elapsed <= 0) return s;\n    double t = Math.min(capacity, s.tokens + elapsed * ratePerNano);\n    return new State(t, now);\n  }\n\n  /** Non-blocking, ALL-OR-NOTHING. refill + test + consume are ONE atomic transition. */\n  boolean tryAcquire(int permits) {\n    if (permits <= 0) throw new IllegalArgumentException();\n    if (permits > capacity) return false;                 // can NEVER be satisfied — fail fast\n    while (true) {\n      State cur = state.get();\n      State r = refilled(cur, clock.getAsLong());\n      if (r.tokens < permits) return false;               // deny: consume nothing\n      State next = new State(r.tokens - permits, r.lastRefillNanos);\n      if (state.compareAndSet(cur, next)) return true;    // commit; on failure re-read EVERYTHING\n    }\n  }\n\n  /** Blocking: compute the deficit, sleep exactly that long, retry. No waiter queue. */\n  boolean acquire(int permits, long timeoutNanos) throws InterruptedException {\n    if (permits > capacity) throw new IllegalArgumentException(\"permits exceed capacity\");\n    long deadline = clock.getAsLong() + timeoutNanos;\n    while (true) {\n      if (Thread.interrupted()) throw new InterruptedException();\n      long now = clock.getAsLong();\n      State cur = state.get();\n      State r = refilled(cur, now);\n      if (r.tokens >= permits) {\n        State next = new State(r.tokens - permits, r.lastRefillNanos);\n        if (state.compareAndSet(cur, next)) return true;\n        continue;                                          // lost the race — recompute, do not sleep\n      }\n      double deficit = permits - r.tokens;\n      long waitNanos = (long) Math.ceil(deficit / ratePerNano);   // EXACTLY when it will be ready\n      if (now + waitNanos > deadline) return false;               // cannot make the deadline\n      LockSupport.parkNanos(Math.min(waitNanos, deadline - now)); // no spin, no busy wait\n    }\n  }\n\n  double availableTokens() { return refilled(state.get(), clock.getAsLong()).tokens; }  // diagnostic\n}\n\n/** Per-key lifecycle: striped, with idle eviction so the map cannot leak keys. */\nfinal class BucketRegistry {\n  private final int stripes;\n  private final ConcurrentHashMap<String, Entry>[] shards;\n  private final double capacity, rate;\n  private final long idleEvictNanos;\n\n  private static final class Entry {\n    final TokenBucket bucket; volatile long lastAccessNanos;\n    Entry(TokenBucket b, long t) { bucket = b; lastAccessNanos = t; }\n  }\n\n  @SuppressWarnings(\"unchecked\")\n  BucketRegistry(int stripes, double capacity, double rate, long idleEvictNanos) {\n    this.stripes = stripes; this.capacity = capacity; this.rate = rate;\n    this.idleEvictNanos = idleEvictNanos;\n    this.shards = new ConcurrentHashMap[stripes];\n    for (int i = 0; i < stripes; i++) shards[i] = new ConcurrentHashMap<>();\n  }\n\n  private ConcurrentHashMap<String, Entry> shardFor(String key) {\n    int h = key.hashCode(); h ^= (h >>> 16);              // spread — raw hashCode clusters\n    return shards[Math.floorMod(h, stripes)];\n  }\n\n  boolean tryAcquire(String key, int permits) {\n    long now = System.nanoTime();\n    Entry e = shardFor(key).computeIfAbsent(key,          // atomic: never two buckets for one key\n        k -> new Entry(new TokenBucket(capacity, rate, System::nanoTime), now));\n    e.lastAccessNanos = now;\n    return e.bucket.tryAcquire(permits);\n  }\n\n  /** Called periodically by ONE shared sweeper — not one timer per bucket. */\n  void evictIdle() {\n    long cutoff = System.nanoTime() - idleEvictNanos;\n    for (ConcurrentHashMap<String, Entry> shard : shards) {\n      // Safe: an evicted bucket is recreated FULL, which is never stricter than the policy.\n      shard.entrySet().removeIf(en -> en.getValue().lastAccessNanos < cutoff);\n    }\n  }\n}"),
      C("deep_dive", "Deep dive: lazy refill — why a timer thread per bucket does not scale",
        "The naive design gives every bucket a ScheduledExecutorService task that increments tokens on a fixed period, and it is worth dissecting exactly why that collapses, because the reasoning generalizes far past rate limiting. COST IS PROPORTIONAL TO KEYS, NOT TRAFFIC. With 40,000 API keys and a 10 ms tick you have scheduled 4,000,000 task executions per second, essentially all of which do nothing useful because only a few hundred keys are active. The scheduler's internal timer queue becomes the bottleneck; ScheduledThreadPoolExecutor uses a DelayQueue whose heap must be touched on every reschedule, so you are paying O(log n) heap operations millions of times a second to add tokens to buckets nobody is calling. IDLE KEYS NEVER STOP COSTING. A key that went quiet three days ago still ticks, which also means its bucket can never be evicted while a live task references it — the timer holds the reference, so your memory leak now has a root that is invisible to a casual reading of the code. QUANTIZATION. A 10 ms tick means a token that mathematically accrued at t+3 ms is not granted until t+10 ms, so your limiter is systematically stricter than configured, and the error grows as the tick coarsens. Trying to fix it by shrinking the tick multiplies the first problem. DRIFT AND CATCH-UP. If the scheduler is busy and a fixed-RATE task falls behind, it fires a burst of catch-up executions and can hand out a clump of tokens at once; using fixed-DELAY instead silently loses rate. Lazy refill deletes all four problems by inverting the responsibility: instead of pushing tokens in, DERIVE them on read. tokensNow = min(capacity, tokens + (now - lastRefillNanos) * ratePerNano). An idle bucket does literally nothing — no thread, no wakeup, no heap entry — and settles its entire three-day debt in one multiply and one min when it is finally touched. The min is the capacity clamp and it is what makes an unbounded idle period safe: the bucket saturates at capacity rather than accumulating three days of tokens and then permitting a catastrophic burst. Two implementation details carry real interview weight. First, tokens must be a DOUBLE (or fixed-point), not an int. With rate 100/sec and a call every 5 ms, elapsed * ratePerNano = 0.5 tokens; truncate to int and every refill is zero, so the bucket empties once and never recovers — a bug that passes any test with sleeps longer than the token interval. Second, ALWAYS UPDATE lastRefillNanos when you update tokens, and never advance it without crediting the tokens for that interval, or elapsed time is silently discarded and the effective rate drops. Finally note the elegance of what remains: eviction now needs ONE shared sweeper for the whole registry, not one timer per bucket, because a bucket carries no independent lifecycle."),
      C("deep_dive", "Deep dive: making check-and-consume atomic, and choosing the clock",
        "The functional core is a READ-MODIFY-WRITE, and every rate limiter bug that is not a refill bug is an atomicity bug. Consider the innocent-looking sequence: refill(); if (tokens >= 1) { tokens -= 1; return true; }. With one token left and two threads arriving together, both can execute refill and both can evaluate tokens >= 1 as true before either decrements — and the limiter grants two permits it did not have. The window is nanoseconds wide, so it never appears in testing and appears constantly at production concurrency, where it manifests as an upstream partner reporting that you exceeded your quota. There are exactly two correct shapes. SYNCHRONIZED: put refill, test, and consume inside one synchronized method. This is genuinely fine — the critical section is a few arithmetic operations, an uncontended intrinsic lock is cheap, and simplicity has real value. Its weakness is that at high contention on a single hot key, threads block and the OS gets involved; and a blocking acquire implemented naively inside a synchronized block would hold the monitor while sleeping, which is catastrophic. CAS RETRY LOOP over an immutable snapshot: read the current State, compute the refilled-and-consumed successor as a NEW object, and compareAndSet. If another thread committed in between, the CAS fails and you recompute from a fresh read — including a fresh clock reading, since your elapsed calculation is also stale. This is why State must be immutable and hold BOTH fields: if tokens and lastRefillNanos were separate atomics you could update one and be preempted before the other, and no reader could tell. Bundling them into one reference is what reduces a two-field update to a one-word CAS. The trade is one small allocation per successful acquire — negligible in a modern generational collector, but worth stating, and avoidable by packing both values into a single long with fixed-point tokens if you are counting nanoseconds. THE CLOCK is the other half of correctness. Use System.nanoTime(): it is MONOTONIC, it is not affected by NTP steps or by the operator changing the system clock, and its origin is arbitrary so only DIFFERENCES are meaningful — which is exactly all this algorithm uses. Use System.currentTimeMillis() and an NTP correction that steps the clock backwards by two seconds makes elapsed NEGATIVE; unguarded, that subtracts tokens or produces nonsense, and even if you clamp it at zero, the bucket refuses to refill until wall-clock time catches back up — a rate limiter that mysteriously wedges for two seconds. A step FORWARD is equally bad in the other direction: it instantly credits a burst of tokens the caller never earned. Two more clock notes that land well: nanoTime differences can be computed safely as (now - then) even across the long overflow boundary because two's-complement subtraction wraps correctly, and injecting the clock as a LongSupplier makes the whole thing unit-testable — you can prove a 100/sec limiter grants exactly 100 permits in a simulated second without a single sleep in the test suite."),
      C("deep_dive", "Deep dive: per-key lifecycle, striping, fairness, and blocking acquire",
        "A single bucket is a toy; the production object is the REGISTRY, and it introduces three problems the single-bucket design never had. UNBOUNDED KEY GROWTH: one bucket per API key, tenant, IP, or user means the map grows with the key space, not the active set. IP-keyed limiters are the classic memory-exhaustion vector — an attacker cycling source addresses creates a bucket per address, and since each is a live object in a strongly-referenced map, the JVM eventually dies. Three eviction strategies, with their trade-offs. (a) SIZE-BOUNDED LRU via a Caffeine cache with maximumSize: predictable memory ceiling, but under a flood the eviction can drop the buckets of legitimate heavy users and reset their limits. (b) IDLE EXPIRY (expireAfterAccess = several times the refill period): evicts exactly what nobody is using and is the usual right answer, since a bucket idle for much longer than capacity/rate has refilled to full anyway and destroying it loses no information. (c) PERIODIC SWEEP by one shared thread — cheap and explicit, and note carefully that this is ONE thread for the whole registry rather than the per-bucket timer the naive design used. The correctness argument for eviction is worth stating out loud because interviewers push on it: an evicted bucket is recreated FULL, so eviction can only ever be more permissive than keeping it — therefore never evict a bucket that is currently in DEBT (empty and recently drained), or you hand an attacker a free reset by simply waiting out the idle window. Bound the idle window well above capacity/rate and that hole closes. CONTENTION AND STRIPING: ConcurrentHashMap already partitions by bin, so a plain map is usually adequate; explicit striping matters when one shard's entries share cache lines or when you want per-shard sweeping without touching the whole map. What striping does NOT fix is a single HOT KEY — all traffic for one tenant hits one bucket's atomic, and if that is genuinely the bottleneck the answer is to shard the tenant's own allowance across N sub-buckets, each with rate/N, accepting slightly looser enforcement in exchange for N times the throughput. FAIRNESS AND STARVATION on the blocking path: this design's acquire computes the deficit and sleeps, then retries — which means it is explicitly NOT FIFO. A thread requesting 50 permits can be repeatedly beaten by threads requesting 1 permit that keep draining the bucket as tokens trickle in, so large requests can starve indefinitely under sustained small-request load. Say this before the interviewer finds it, then give the options: cap the maximum permits per request so the disparity is bounded; add a fair queue where a waiting large request RESERVES tokens as they accrue (which is what Guava's RateLimiter effectively does by handing out a future execution time and letting the caller sleep — the elegant trick of charging the CURRENT caller for the PREVIOUS caller's debt); or simply document the limiter as unfair, which is the right call for the common case where every request costs one permit. Also note the interaction with capacity: a request for more permits than capacity can never be satisfied and must fail fast rather than block forever, which is why that guard appears at the top of both methods."),
      C("tradeoffs", "Trade-offs & extension points",
        "SYNCHRONIZED vs CAS LOOP: the synchronized bucket is shorter, allocation-free, and easier to prove correct; at low-to-moderate contention its cost is a handful of nanoseconds and it is the right default. The CAS loop never blocks, so a thread preempted mid-update cannot stall others, and it degrades more gracefully on a hot key — but it allocates a State per successful acquire and can livelock into repeated retries at extreme contention (mitigate with onSpinWait or brief backoff). Pick by measurement, and say so. LAZY REFILL vs SCHEDULED REFILL: lazy wins on every axis that matters at scale (zero idle cost, exact rather than quantized, evictable); the only thing a scheduled refill offers is that the token count is 'current' without a read, which nothing needs. The one legitimate use of a shared scheduler here is registry eviction. NON-BLOCKING vs BLOCKING API: tryAcquire is the safer default because it pushes the decision to the caller — shed load, return 429 with a Retry-After computed from the deficit, or queue at a higher level. acquire() blocks a request thread, which under sustained overload converts a rate-limit into a thread-pool exhaustion and then a cascading failure, so expose it only with a mandatory timeout. In an asynchronous stack, the better shape is a CompletableFuture scheduled at the computed ready-time, which throttles without holding a thread at all. UNFAIR vs FAIR: unfair is faster and simpler and fine when all requests cost one permit; fairness (reservation, or a FIFO waiter queue) is required when permit costs vary widely, and costs a queue plus a wakeup protocol. IN-PROCESS vs DISTRIBUTED: this bucket limits ONE JVM. Across N instances the naive fix — rate/N per instance — is wrong under uneven load balancing and wastes allowance when an instance is idle. The real answers are a shared Redis bucket updated by an atomic Lua script (one round trip on the hot path, and the limiter is now a hard dependency with its own availability), or approximate local enforcement with periodic allowance redistribution from a central coordinator — cheap and eventually correct, which is usually what the business actually needs. WHAT BREAKS AT SCALE: unbounded key maps (fix with idle eviction, and never evict a bucket in debt); a single hot key serializing on one atomic (fix by sub-sharding that key's allowance); double truncation errors from integer tokens; blocking acquire exhausting the request thread pool; and clock assumptions — nanoTime is monotonic within a JVM but NOT comparable across machines, which is exactly why the distributed variant must keep its time source server-side. EXTENSIONS worth naming: multi-tier limits (per-second and per-day buckets checked together, denying if EITHER refuses), cost-weighted permits so an expensive endpoint charges more tokens, and a Retry-After header computed directly from the deficit, which is free here because the design already knows exactly when the next token arrives."),
    ],
    "In a concurrency interview the token bucket is not a question about rate-limiting policy — it is a question about whether you can build a correct shared-mutable primitive. The interviewer usually lets you write the naive version, then asks what happens with 40,000 keys, expecting you to recognize immediately that a timer thread per bucket is the wrong shape and that refill should be derived from elapsed time on read. The next probe is atomicity: they will describe two threads and one remaining token and ask you to walk the interleaving, then expect either a synchronized critical section covering refill-test-consume together, or a CAS loop over an immutable (tokens, lastRefillNanos) snapshot — and expect you to explain why the two fields must move together. The clock question follows: nanoTime versus currentTimeMillis, and what an NTP step backwards does to your elapsed calculation. Then lifecycle: how the per-key map avoids unbounded growth, and why an evicted bucket must not hand out a free reset. Strong candidates volunteer the fairness gap in blocking acquire — that a 50-permit request can starve behind a stream of 1-permit requests — and offer reservation or a permit cap rather than waiting to be told.",
    [
      G("timer_thread_per_bucket", "Adding tokens from a scheduled task per bucket, so cost scales with the number of KEYS rather than with traffic, idle buckets never stop ticking, and the live timer reference makes the map unevictable.", "Derive the refill on read: tokens = min(capacity, tokens + (now - lastRefillNanos) * ratePerNano). An idle bucket then costs nothing and settles its whole debt in one multiply, and eviction needs one shared sweeper for the whole registry instead of a timer per bucket.", "ScheduledExecutorService.scheduleAtFixedRate inside a bucket constructor; scheduler thread saturation as key count grows; heap dumps dominated by bucket objects retained by timer tasks."),
      G("non_atomic_check_and_consume", "Refilling, then testing tokens >= n, then decrementing as three separate steps — so two threads with one token left both observe it as available and both consume it.", "Fuse refill, test, and consume into one atomic transition: a single synchronized block, or a CAS retry loop that swaps an IMMUTABLE (tokens, lastRefillNanos) snapshot so both fields move together. On CAS failure re-read everything including the clock.", "Separate atomics for tokens and lastRefill; an AtomicLong decrementAndGet after a separate get; granted-permit counts that exceed the configured rate only under production concurrency."),
      G("wall_clock_and_int_tokens", "Using System.currentTimeMillis for elapsed time, and/or storing tokens as an int — so an NTP step backwards wedges the limiter and sub-token accrual truncates to zero.", "Use System.nanoTime (monotonic; only differences are meaningful, and the subtraction is safe across overflow) and store tokens as a double or fixed-point. Inject the clock as a LongSupplier so the limiter is testable without sleeping.", "currentTimeMillis in a refill calculation; a negative elapsed guard that was added after an incident; a 100/sec limiter sampled every 5 ms that grants nothing after the first burst."),
    ],
    0.6, DIAG.tokenbucket, "Token bucket rate limiter class diagram"),

  // ─────────────────── ACTOR SYSTEM ─────────────────────────────────────────
  TT("lld_m20", "lld_m20_t8", 8, "Design an Actor System", "design-actor-system",
    ["case-study", "concurrency", "message-passing", "supervision"],
    "A multiplayer game server keeps 100,000 live sessions. Every session has mutable state — position, inventory, health — touched by a network thread, a physics tick, and a chat handler. The lock-based version accumulated eleven locks, two deadlocks that only appear under load, and a rule nobody can remember about acquisition order. Someone proposes deleting every lock in the codebase. Not replacing them — DELETING them. Design the runtime that makes that safe, and then answer the question that decides whether you understand it: what exactly guarantees that an actor's fields need no synchronization at all?",
    "The actor model buys lock-freedom with one structural invariant: state is PRIVATE to an actor, and the only thing that crosses an actor boundary is an immutable message. If no two threads can ever reach the same field, no field needs protecting — the data race is eliminated by construction rather than defended against. But that invariant alone is not enough, and this is the part candidates miss: it holds only because the runtime guarantees that ONE ACTOR PROCESSES AT MOST ONE MESSAGE AT A TIME. The dispatcher may run 100,000 actors on eight threads, but it never schedules the same actor on two of them simultaneously, so an actor's receive() method is effectively single-threaded and its fields are thread-confined. That single scheduling rule is what converts 'messages instead of locks' from a slogan into a correctness argument — and the moment you block inside receive(), you steal one of those eight shared threads and the whole scheme degrades.",
    [
      C("requirements", "Requirements",
        "Functional: (1) spawn(behavior, name) creates an actor and returns an ActorRef — never the actor object itself, because handing out the instance would let a caller invoke methods directly and destroy the entire isolation guarantee. (2) tell(message) — asynchronous, fire-and-forget send that returns immediately; this is the primary and preferred interaction. (3) An ask/request-response facility returning a Future, built ON TOP of tell rather than being a primitive, because request-response in a message system is just two messages plus a correlation. (4) Mailbox per actor with a defined capacity and a defined overflow policy. (5) A supervision hierarchy: every actor except the root has a parent, and when a child fails its parent decides to restart it, stop it, resume it, or escalate. (6) Lifecycle hooks — preStart, postStop, preRestart — so an actor can acquire and release resources deterministically. (7) Graceful shutdown that drains or discards mailboxes on a deadline. Non-functional requirements, which are where the design decisions come from. SCALE: 100,000 actors on 8 to 16 threads, so an actor must cost kilobytes of heap and NOT a thread — a thread-per-actor design is disqualified at the requirements stage, before anyone writes code. ISOLATION: it must be structurally impossible for two threads to touch one actor's state, and 'impossible' is stronger than 'discouraged'. BACK-PRESSURE: an unbounded mailbox turns a slow consumer into an OutOfMemoryError, so bounded mailboxes with an explicit policy are a requirement rather than a tuning knob. FAULT ISOLATION: one actor throwing must not corrupt or halt any other actor. ORDERING: a precise, MINIMAL guarantee — messages from a given sender to a given receiver arrive in send order; nothing is promised globally, and over-promising here is a design error. Explicitly out of scope: distribution across machines (named in trade-offs, where at-most-once versus at-least-once delivery becomes the central question), and persistence/event sourcing."),
      C("entities", "Core entities & responsibilities",
        "ACTOR is a behaviour plus PRIVATE mutable state and one method, receive(Object message). Its fields are ordinary, unsynchronized, non-volatile fields — and that is the payoff of the whole design, not an oversight. The class must never expose a public method that mutates state, and the runtime must never hand out a reference to the instance. ACTORREF is the address and the only handle anyone outside ever holds: it exposes tell(msg, sender) and nothing else. This indirection is the enforcement mechanism for isolation, and it is also what makes location transparency possible later — a ref can point at a local mailbox or a remote node without the caller knowing. MAILBOX is the per-actor queue (an ArrayBlockingQueue for bounded, ConcurrentLinkedQueue for unbounded) plus a SCHEDULED flag, usually an AtomicBoolean, that records whether this actor is currently queued for or running on a dispatcher thread. That flag is the mechanism that enforces one-message-at-a-time and deserves to be called out explicitly. DISPATCHER is the scheduler: a fixed thread pool plus the logic that multiplexes N actors onto M threads. When a message is enqueued into an idle actor's mailbox, the dispatcher submits a task that drains up to a THROUGHPUT budget of messages and then reschedules — the budget existing so one busy actor cannot monopolize a pool thread. MESSAGE should be an immutable value object; if a mutable object is sent, both actors can reach it and the isolation argument silently collapses, which is why real systems either require immutability by convention or copy on send. SUPERVISOR is not a separate class but a RESPONSIBILITY of the parent actor: a SupervisorStrategy maps a Throwable to a Directive (resume, restart, stop, escalate). ACTORSYSTEM owns the dispatcher, the root guardian actor, the name registry, and shutdown. DEADLETTERS is the sink for messages to stopped or unknown actors — a named entity because 'the message silently vanished' is otherwise an unfalsifiable production mystery."),
      C("design", "Class design: isolation, the scheduling invariant, and no locks anywhere",
        "The design has exactly one big idea and several small mechanisms that protect it. THE BIG IDEA: replace shared memory with message passing, so mutual exclusion is achieved by data OWNERSHIP rather than by locks. Each mutable thing has exactly one owner, and other components ask the owner to act rather than reaching in. THE MECHANISM THAT MAKES IT TRUE: the scheduled flag. When tell() enqueues into a mailbox, it then attempts scheduled.compareAndSet(false, true); ONLY the thread that wins that CAS submits a run task to the dispatcher. Any other concurrent sender simply enqueues and returns, because the actor is already scheduled. The running task drains messages, then sets scheduled to false and — crucially — re-checks whether the mailbox became non-empty during that window, rescheduling if so. That check closes the lost-schedule race in which a message arrives after the drain loop's last poll but before the flag is cleared, which would otherwise leave a non-empty mailbox with nobody scheduled to process it: an actor that silently stops responding. Together those two CAS steps give the invariant the whole model rests on — at most one thread executes any given actor's receive() at any instant. THE ASYNCHRONY: tell() must NOT execute receive() on the caller's thread. Doing so would reintroduce concurrent access, turn a send into a synchronous call with the callee's latency, and make deadlock possible again. Patterns, named honestly: ACTIVE OBJECT is the pattern this is (method invocation decoupled from execution via a queued request and a scheduler); the ActorRef is a PROXY; the SupervisorStrategy is STRATEGY; the message dispatch inside receive is usually pattern-matching, which in Java is a switch over sealed types. What is deliberately ABSENT: no locks, no synchronized, no volatile on actor state — the memory visibility comes for free because the queue's own happens-before edge (a write to a concurrent queue happens-before the read that dequeues it) carries the sender's data safely, and consecutive executions of the same actor on different pool threads are chained by that same edge."),
      K("code", "Core classes (Java)",
        "// ── The only handle callers ever hold. Hides the Actor instance entirely. ──\ninterface ActorRef { void tell(Object msg, ActorRef sender); String path(); }\n\nabstract class Actor {\n  ActorContext context;                                 // set by the system on spawn\n  protected abstract void receive(Object msg) throws Exception;\n  protected void preStart() {}\n  protected void postStop() {}\n  protected void preRestart(Throwable cause, Object failedMsg) {}\n}\n\nenum Directive { RESUME, RESTART, STOP, ESCALATE }\ninterface SupervisorStrategy { Directive decide(Throwable t); }\n\n// ── Mailbox + scheduling flag: the one-message-at-a-time invariant lives here. ──\nfinal class Cell implements ActorRef {\n  private final String path;\n  private final ActorSystem system;\n  private final Supplier<Actor> factory;                 // used again on RESTART\n  private final BlockingQueue<Envelope> mailbox;         // BOUNDED — back-pressure by default\n  private final AtomicBoolean scheduled = new AtomicBoolean(false);\n  private final SupervisorStrategy strategy;\n  private final ActorRef parent;\n  private volatile Actor actor;                          // replaced on restart\n  private volatile boolean stopped = false;\n  private static final int THROUGHPUT = 32;              // messages per dispatch slice\n\n  Cell(String path, ActorSystem system, Supplier<Actor> factory, int capacity,\n       SupervisorStrategy strategy, ActorRef parent) {\n    this.path = path; this.system = system; this.factory = factory;\n    this.mailbox = new ArrayBlockingQueue<>(capacity);\n    this.strategy = strategy; this.parent = parent;\n    this.actor = factory.get(); this.actor.preStart();\n  }\n\n  @Override public String path() { return path; }\n\n  @Override public void tell(Object msg, ActorRef sender) {\n    if (stopped) { system.deadLetters(msg, this, sender); return; }\n    if (!mailbox.offer(new Envelope(msg, sender))) {      // FULL: an explicit policy, never a hang\n      system.onMailboxOverflow(this, msg);                // drop / deadLetter / fail fast\n      return;\n    }\n    schedule();\n  }\n\n  private void schedule() {\n    // ONLY the thread that flips false->true submits — everyone else just enqueued and leaves.\n    if (scheduled.compareAndSet(false, true)) system.dispatcher().execute(this::run);\n  }\n\n  private void run() {\n    try {\n      for (int i = 0; i < THROUGHPUT; i++) {              // budget: do not hog the pool thread\n        Envelope e = mailbox.poll();\n        if (e == null) break;\n        processOne(e);\n        if (stopped) break;\n      }\n    } finally {\n      scheduled.set(false);\n      // Close the lost-schedule race: a message may have landed after the last poll.\n      if (!mailbox.isEmpty() && !stopped) schedule();\n    }\n  }\n\n  private void processOne(Envelope e) {\n    Actor a = actor;\n    try {\n      a.receive(e.message);                              // NO locks: only this thread can be here\n    } catch (Throwable t) {\n      applyDirective(strategy.decide(t), t, e.message);   // failure is a decision, not a crash\n    }\n  }\n\n  private void applyDirective(Directive d, Throwable t, Object failedMsg) {\n    switch (d) {\n      case RESUME -> { /* keep state, drop the offending message, carry on */ }\n      case RESTART -> {\n        actor.preRestart(t, failedMsg);\n        actor.postStop();\n        Actor fresh = factory.get();                     // discard corrupted state; mailbox SURVIVES\n        fresh.context = actor.context;\n        fresh.preStart();\n        actor = fresh;\n      }\n      case STOP -> stop();\n      case ESCALATE -> { stop(); if (parent != null) parent.tell(new Failed(this, t), this); }\n    }\n  }\n\n  void stop() {\n    stopped = true;\n    try { actor.postStop(); } catch (Throwable ignored) {}\n    Envelope e;\n    while ((e = mailbox.poll()) != null) system.deadLetters(e.message, this, e.sender);\n  }\n\n  record Envelope(Object message, ActorRef sender) {}\n  record Failed(ActorRef child, Throwable cause) {}\n}\n\n// ── ask(): request/response built ON TOP of tell, never as a primitive. ─────\nfinal class Ask {\n  static CompletableFuture<Object> ask(ActorSystem sys, ActorRef target, Object msg, Duration timeout) {\n    CompletableFuture<Object> promise = new CompletableFuture<>();\n    ActorRef temp = sys.spawnTemp(reply -> promise.complete(reply));   // one-shot reply address\n    sys.scheduler().schedule(\n        () -> promise.completeExceptionally(new TimeoutException(\"ask timed out\")),\n        timeout.toMillis(), TimeUnit.MILLISECONDS);                    // ALWAYS bound the wait\n    target.tell(msg, temp);\n    return promise;                                                    // do NOT call get() in an actor\n  }\n}"),
      C("deep_dive", "Deep dive: the invariant that makes unsynchronized state safe",
        "Ask a candidate why an actor's fields need no synchronization and most answer 'because actors do not share state'. That is the premise, not the argument, and the interviewer is looking for the mechanism underneath. THE FULL ARGUMENT has two halves. Half one, ISOLATION: no reference to the actor instance ever escapes — callers hold an ActorRef, which exposes only tell — so no other component can invoke a method on the actor or read its fields. Half two, SERIAL EXECUTION: the runtime guarantees that at most one thread executes a given actor's receive() at any instant. Without half two, half one is worthless: 100,000 actors sharing eight pool threads would still let two threads pick up two messages for the SAME actor and run receive() concurrently on the same unsynchronized fields — a plain data race, with the actor abstraction merely hiding it. So the scheduling invariant is not an optimization, it is a correctness requirement, and the mechanism that provides it is the scheduled AtomicBoolean. Walk the protocol precisely. A sender enqueues an envelope, then attempts compareAndSet(false, true). Exactly one thread can win that CAS, and only the winner submits a task to the dispatcher; concurrent senders see it already scheduled, enqueue, and return. The running task drains up to a throughput budget, then sets the flag false. Now the subtle part, and the bug every hand-rolled implementation ships: between the drain loop's last failed poll and the flag being cleared, another thread can enqueue a message AND see scheduled still true, so it will not submit a task. The message would then sit in a non-empty mailbox with nobody scheduled to run it — an actor that silently goes deaf, usually diagnosed weeks later as 'occasionally a session stops responding'. The fix is in the finally block: after clearing the flag, re-check isEmpty() and reschedule if the mailbox is non-empty. The re-check may occasionally schedule a redundant no-op drain, which is harmless; the reverse mistake is not. MEMORY VISIBILITY is the other half people forget. When actor state is touched by thread T1 during one dispatch slice and by thread T2 during the next, the fields are plain and non-volatile — why does T2 see T1's writes? Because the concurrent mailbox provides a happens-before edge: T1's writes precede its enqueue/flag operations, which are atomic operations on shared state, which precede T2's corresponding reads. The actor never has to reason about the JMM because the runtime's queue and CAS operations establish the ordering. State that clearly — 'the mailbox's happens-before edge chains consecutive dispatch slices' — and you have answered a question most candidates do not even know is being asked."),
      C("deep_dive", "Deep dive: mailboxes, back-pressure, and the blocking hazard",
        "THE MAILBOX is where an actor system meets physics, and the first decision is bounded versus unbounded. Unbounded is the seductive default: sends never fail, no policy to write, no caller ever surprised. It is also how actor systems die. If a producer sustains a higher rate than an actor can process — and the model makes that easy, since tell() returns immediately and gives the caller no feedback — the queue grows until the heap is gone. The failure is especially nasty because the symptom appears far from the cause: the process OOMs, and the actor that was slow is not obviously implicated. BOUNDED mailboxes force the question to be answered at design time, and the overflow policy must be an explicit choice with a stated rationale. DROP NEWEST is right for telemetry and sensor readings where the freshest value matters least once you are behind. DROP OLDEST fits status updates where only the latest is meaningful. DEAD-LETTER AND CONTINUE keeps the system alive and makes the loss observable. FAIL THE SENDER converts an asynchronous overflow into a synchronous error the caller can act on. BLOCK THE SENDER is the one that looks like real back-pressure and is usually a trap: if the sender is itself an actor, it now occupies a dispatcher thread while blocked, and if the receiver is downstream of that same pool you have a distributed deadlock that only appears at saturation. The principled answer is that back-pressure belongs in the PROTOCOL — a demand-based exchange in which the consumer tells the producer how many messages it is willing to accept (exactly what Reactive Streams formalizes) — so flow control is expressed as messages instead of as blocked threads. THE BLOCKING HAZARD deserves its own treatment because it is the number one production failure in actor systems. Inside receive(), a JDBC call, an HTTP request, a Thread.sleep, or a Future.get() does not merely slow that actor down: it OCCUPIES A SHARED DISPATCHER THREAD. With eight threads and eight actors blocked on a slow database, the entire system of 100,000 actors stops — none of them are blocked themselves, but there is nobody left to run them. Worse, the actor that would complete the future may itself be waiting for a thread, which is a genuine self-deadlock. The remedies, in order of preference: use asynchronous APIs and pipe the resulting CompletionStage back to self as a message (Akka's pipeTo, and note that the completion handler must NOT touch actor state directly — it must send a message, or you have broken serial execution); or, if blocking is unavoidable, isolate those actors on a DEDICATED dispatcher with its own threads, so the blast radius is bounded to that pool. The throughput budget in the dispatch loop is the related fairness control: without it, one actor with a saturated mailbox holds a pool thread indefinitely and starves every other actor assigned to it."),
      C("deep_dive", "Deep dive: ordering guarantees, supervision, and let-it-crash",
        "ORDERING is the guarantee interviewers probe most sharply because it is the one candidates over-promise. What an actor system provides: messages from ONE sender to ONE receiver are delivered in send order. That is it. It follows from a FIFO mailbox and a single sender thread, and it is genuinely useful — a client's commands are never reordered relative to each other. What is NOT provided: any global order. If A sends m1 to C and B sends m2 to C, C may see them in either order regardless of wall-clock send times. Transitivity also fails, and this is the classic trap: A tells B, then A tells C, and B tells C — there is no guarantee C sees B's message after A's, because those are different sender/receiver pairs travelling different paths. Any protocol needing causal order must carry it explicitly (sequence numbers, vector clocks, or routing through a single coordinating actor). Delivery locally is at-most-once — a mailbox overflow or a stopped actor drops the message to dead letters — so anything requiring reliability needs acknowledgements and retries at the application level, which in turn requires IDEMPOTENT handlers, since a retry may duplicate. SUPERVISION is the actor model's answer to failure, and its philosophy is genuinely different from defensive programming. LET IT CRASH says: do not litter receive() with try/catch attempting to repair state you cannot reason about; let the exception propagate, and let the PARENT — which has context the child lacks — decide. The parent's SupervisorStrategy maps the throwable to a directive. RESUME keeps the actor's state and drops the offending message, appropriate for a bad input that did not corrupt anything. RESTART discards the actor instance and constructs a fresh one from the same factory — and the crucial detail is that THE MAILBOX SURVIVES, so queued messages are processed by the new instance and the actor's identity, its ActorRef, is unchanged; nobody holding a reference needs to learn about the restart. STOP terminates it and dead-letters the remainder. ESCALATE hands the failure to the grandparent, which is how a systemic problem (a downed database) propagates up to someone with the authority to shut down a whole subtree rather than restarting a leaf a thousand times a second. Two refinements earn credit: an exponential-backoff supervisor prevents that restart storm, and preRestart/postStop hooks are where resources are released so a restart does not leak file handles or sockets. The deeper point to articulate is architectural — supervision makes failure handling a FIRST-CLASS, LOCATED concern (a tree, with explicit policies at each node) instead of an ad-hoc scattering of catch blocks, which is exactly why the model survived into distributed systems where partial failure is guaranteed rather than exceptional."),
      C("tradeoffs", "Trade-offs & extension points",
        "ACTORS vs LOCKS + SHARED STATE: actors eliminate data races and lock-ordering deadlocks by construction and scale to hundreds of thousands of entities on a handful of threads. The costs are real and must be named. Type safety weakens — a receive(Object) with a switch is unchecked, which is why Akka Typed exists and why a modern answer mentions sealed interfaces and pattern matching. Debugging is harder: stack traces stop at the mailbox boundary, so causality must be reconstructed from correlation ids and tracing. Any operation spanning two actors is no longer atomic; if you need a transaction across two actors you must build a protocol (a saga, or a two-phase commit) — an interviewer will absolutely ask 'transfer money between two account actors', and the answer is a coordinator actor plus compensation, not a lock. And latency per interaction rises, since every call becomes a queue hop. ACTORS vs CSP (Go channels): actors name the RECEIVER and own an implicit inbox; CSP names the CHANNEL, which can have multiple readers and offers rendezvous semantics. Channels compose more naturally with select; actors carry identity and lifecycle, which is what makes supervision possible. ACTORS vs EVENT LOOP: one loop is a single-actor system with one mailbox — the actor model generalizes it to N independent state machines that can use the whole machine. ACTORS vs VIRTUAL THREADS: Loom makes blocking cheap again, so the sequential programming model with a lock per entity becomes viable at this scale; the counter-argument is that actors still give supervision, location transparency, and the absence of lock-ordering rules, none of which virtual threads provide. BOUNDED vs UNBOUNDED MAILBOX: covered above — bounded by default, unbounded only where the producer is provably rate-limited elsewhere. SHARED vs DEDICATED DISPATCHER: one shared pool is efficient and is the default; a dedicated pool is mandatory for actors that must block, and a pinned single-thread dispatcher is used when an actor wraps a thread-affine resource. DISTRIBUTION: because the ActorRef is already an indirection, making it remote is a smaller change than in any lock-based design — this is location transparency, and it is the model's headline advantage. But it is not free, and pretending otherwise is the mistake: remote sends require serialization (so messages must be serializable, not merely immutable), delivery drops from at-most-once-locally to genuinely unreliable, latency changes by orders of magnitude, and message size starts to matter. WHAT BREAKS AT SCALE: a single hot actor is a serialization point with no parallelism available — the fix is sharding an entity across N actors by key, or splitting read-only work out. Unbounded mailboxes OOM. Blocking in receive exhausts the shared pool. Restart storms without backoff burn CPU. And dead letters must be MONITORED, because in an at-most-once system an unmonitored dead-letter queue means messages vanish with no trace. EXTENSIONS worth naming: event sourcing (persist the message log, rebuild state by replay — a natural fit since an actor is already a state machine driven by messages), cluster sharding for placing entity actors across nodes, and a receive-timeout for idle passivation so 100,000 sessions do not all stay resident."),
    ],
    "Actor systems come up in senior interviews as the 'design a concurrent runtime' question, and the grading is less about API surface than about whether you can state the guarantee that makes it safe. The opening is usually a scale statement — 100,000 stateful entities, a handful of threads — and the first checkpoint is that you reject thread-per-actor immediately and describe multiplexing. The core probe follows: why does an actor's mutable state need no synchronization? The complete answer is isolation (no reference escapes; only an ActorRef is handed out) PLUS the runtime invariant that one actor processes at most one message at a time, and the best candidates go on to name the mechanism — a scheduled flag flipped by CAS, with the re-check after clearing it that closes the lost-schedule race. Then come the operational questions: bounded versus unbounded mailboxes and what overflow policy you choose; what happens when someone calls a blocking database API inside receive (it steals a shared dispatcher thread and can stall every actor in the system); what ordering you actually guarantee (per sender-receiver pair only, never global, never transitive); and how failure is handled (let it crash, parent supervision, restart with the mailbox and the ActorRef surviving). A strong close names the honest costs — weak typing, hard debugging, no cross-actor atomicity — and says when locks or virtual threads would be the better tool.",
    [
      G("no_serial_execution_guarantee", "Believing message passing alone makes actor state safe, and building a dispatcher that can run two messages for the SAME actor on two pool threads concurrently.", "Isolation is only half the argument. Enforce serial execution with a per-actor scheduled AtomicBoolean: only the thread that wins compareAndSet(false, true) submits a dispatch task, and after clearing the flag the drain loop must re-check the mailbox and reschedule if it is non-empty.", "A dispatcher that submits a task per message rather than per actor; actor fields being made volatile or synchronized to fix intermittent corruption; an actor that occasionally stops responding while its mailbox is non-empty."),
      G("blocking_inside_receive", "Calling a database, an HTTP client, Thread.sleep, or Future.get inside receive() because the actor itself is only doing one thing at a time.", "The actor is not blocked — the shared DISPATCHER THREAD is. Eight blocked actors can stall all 100,000. Use async APIs and pipe the CompletionStage result back to self as a message (never mutate actor state from the completion callback), or isolate blocking actors on a dedicated dispatcher.", "JDBC or synchronous HTTP calls in receive; ask(...).get() inside an actor; system-wide latency collapse whenever one downstream dependency slows down."),
      G("assumed_global_ordering", "Assuming messages arrive in global send order, or that ordering is transitive — A tells B and then C, B tells C, so C must see A before B.", "The only guarantee is FIFO per sender-receiver PAIR. Nothing is promised across different pairs, and causality is not transitive. Carry ordering explicitly with sequence numbers or route through one coordinating actor; delivery is at-most-once, so reliability needs acks, retries, and idempotent handlers.", "Protocols that depend on cross-actor timing; missing correlation ids; a saga written as if two actors could update atomically; an unmonitored dead-letter queue."),
    ],
    0.6, DIAG.actorsystem, "Actor system class diagram"),

  // ─────────────────── BLACKJACK ────────────────────────────────────────────
  TT("lld_m21", "lld_m21_t1", 1, "Design Blackjack", "design-blackjack",
    ["case-study", "game-design", "state-machine", "strategy-pattern"],
    "Design blackjack and almost every candidate writes the same first method: loop over the cards, add each rank's value to a running int, treat an Ace as 11, and subtract 10 at the end if the total went over. It looks right. Deal A-A-9 and it returns 21 — correct by luck. Deal A-A-A-8 and it returns 21 too, when the hand is actually 11 plus 8 equals 21... or is it? Work it by hand and you will find the naive fix is wrong for some hands and the 'subtract 10 while busting' patch is wrong for others. The Ace is a two-valued card, and a single accumulated integer cannot represent a two-valued card. That one modelling decision cascades into every rule in the game — the dealer's soft-17, the payout for a blackjack versus a drawn 21, and whether a split hand can be a blackjack at all.",
    "Model a hand's value as a PAIR, not a number: the HARD total (every Ace counted as 1) and the SOFT total (one Ace promoted to 11, valid only when it does not bust). Every rule in blackjack reads cleanly off that pair — the best value is the soft total when it is at most 21, otherwise the hard total; a hand is SOFT when the promotion is live, which is exactly the flag the dealer needs to implement 'hit on soft 17'; and a bust is hard total over 21, full stop. At most one Ace can ever be worth 11 (two would be 22), so the pair is complete — there is no third case. Once value is a pair, the rest of the design is about the fact that the atomic unit of play is the HAND, not the player: splits create new hands, each with its own bet, its own decisions, and its own resolution against the dealer.",
    [
      C("requirements", "Requirements",
        "Functional, stated as the rules that actually drive class structure. Card values: 2-10 face value, J/Q/K count 10, ACE counts 1 OR 11 — the whole design pivots on that last clause. Round flow: players place bets, each receives two cards face up, the dealer receives one up and one down (the hole card), players act in turn, then the dealer plays, then hands are resolved and paid. Player actions: HIT, STAND, DOUBLE DOWN (double the bet, receive exactly one more card, then stand — allowed only on the first two cards), SPLIT (a pair may be separated into two hands, each receiving a second card and each carrying an additional bet equal to the original), and SURRENDER where the house offers it (forfeit half the bet and end the hand). INSURANCE: when the dealer's up card is an Ace, players may wager up to half their bet that the hole card is a ten, paying 2:1 if it is. Dealer behaviour is deterministic and rule-driven: draw until the total is at least 17, with the SOFT-17 rule (hit or stand on a soft 17) as a configurable house option because both variants exist in real casinos and hard-coding either is a modelling failure. BLACKJACK is an Ace plus a ten-value card as the FIRST TWO CARDS, pays 3:2 (or 6:5 in some houses — again configurable), and beats an ordinary 21; a 21 formed after a split is conventionally NOT a blackjack, which is precisely the kind of rule that must live in a named place rather than being implied by an if. Non-functional: the SHOE holds multiple decks (commonly 6 or 8) with a cut card that triggers a reshuffle rather than shuffling every round; the game must be DETERMINISTICALLY REPRODUCIBLE for testing, meaning the randomness source is injected, not created internally; house rules must be data rather than code so a variant does not fork the class; and the money path must be exact, so bets and payouts use integer cents or BigDecimal, never double. Out of scope: multi-table concurrency, persistence, and card-counting countermeasures beyond the cut card."),
      C("entities", "Core entities & responsibilities",
        "CARD is an immutable value object of Rank and Suit. Rank is an enum, and the deliberate choice is that Rank does NOT carry a single int value — it carries the base value with ACE handled specially, because encoding an Ace as 11 (or as 1) in the enum is where the bug is born. SUIT exists for display only; blackjack rules never consult it, and saying that out loud shows you know which parts of the domain are decoration. DECK is 52 cards; SHOE aggregates N decks plus a shuffle, a deal pointer, and a CUT CARD position — the shoe is the entity that owns randomness, and it takes a Random (or a shuffle Strategy) in its constructor so tests can supply a seeded or scripted one. HAND is the CENTRAL entity, and getting this right is most of the design: it owns a list of cards, its own bet, flags for doubled and for whether it came from a split, and it computes its own HandValue. The atomic unit of play in blackjack is the hand, not the player — a player who splits twice has three hands, each with its own money, its own decisions, and its own comparison against the dealer. HANDVALUE is a small immutable value object (hardTotal, softTotal or an isSoft flag) with derived queries best(), isSoft(), isBust(), and isBlackjack(); making it a first-class type rather than an int is the single decision that keeps Ace handling out of every other class. PLAYER owns a bankroll and a list of Hands; DEALER is a Player-like participant with no bankroll of its own and no free choices — its behaviour is a DEALERSTRATEGY. DEALERSTRATEGY is the Strategy interface with the soft-17 flag as configuration, which is what lets a rule variant be a constructor argument instead of a subclass. HOUSERULES is the configuration object collecting blackjackPayout, hitSoftSeventeen, maxSplits, doubleAfterSplit, surrenderAllowed, and insuranceOffered — its existence is the difference between a design that supports variants and one that hard-codes a single casino. ROUND (or Table) is the state machine driving the phases, and PAYOUTRESOLVER computes outcomes per hand."),
      C("design", "Class design: the hand as the unit, and rules as data",
        "Two design decisions carry this system, and both are about placing knowledge where it belongs. FIRST: value logic lives in HandValue, computed from the cards, never accumulated as the hand is built. The naive alternative — maintaining a running total as cards are added, with an aceCount and adjustments — is stateful, order-dependent, and must be re-derived after every mutation anyway; computing from the immutable card list on demand is simpler and always correct. The computation is: sum every card with Aces counted as 1 (the hard total), then if there is at least one Ace and hard + 10 <= 21, the soft total is hard + 10 and the hand is SOFT. best() returns the soft total when it exists, otherwise the hard total. Every rule in the game then reads off that pair, with no Ace logic anywhere else in the codebase. SECOND: the hand, not the player, is the unit of play. Player holds List<Hand>, and the action loop iterates hands, not players. Splitting is therefore a natural operation — remove the second card from the hand, create a new Hand with it, place an equal bet from the bankroll, deal one card to each — rather than a special case bolted onto a player-shaped model. Candidates who model a player as having ONE hand discover at the split requirement that the whole action loop, the resolution logic, and the payout code must be rewritten, which is exactly the trap the requirement is there to spring. PATTERNS, justified rather than listed: STRATEGY for DealerStrategy (a rule variant becomes a configuration value, and a bot player plugs into the same interface for simulation); STATE, or at minimum an explicit enum-driven state machine, for the round phases BETTING, DEALING, INSURANCE, PLAYER_ACTIONS, DEALER_PLAY, PAYOUT — because 'can this player double down right now' is a question about phase AND hand history, and scattering those conditions across if-statements is how illegal moves slip through; a small COMMAND or enum Action for player moves so the same code path serves a UI, a bot, and a replay log; and dependency injection of the shuffle source for determinism. What is deliberately ABSENT: no inheritance hierarchy of hand types (a BlackjackHand subclass buys nothing — it is a query on cards), no observer framework unless the requirements actually name spectators, and no separate class per action."),
      K("code", "Core classes (Java)",
        "enum Suit { HEARTS, DIAMONDS, CLUBS, SPADES }   // display only — rules never read it\n\nenum Rank {\n  TWO(2), THREE(3), FOUR(4), FIVE(5), SIX(6), SEVEN(7), EIGHT(8), NINE(9), TEN(10),\n  JACK(10), QUEEN(10), KING(10), ACE(1);          // ACE stored as 1; the 11 is a PROMOTION\n  final int hardValue;\n  Rank(int v) { this.hardValue = v; }\n  boolean isAce() { return this == ACE; }\n  boolean isTenValue() { return hardValue == 10; }\n}\n\nrecord Card(Rank rank, Suit suit) {}\n\n/** The whole Ace problem, solved once, in one place. */\nfinal class HandValue {\n  final int hard;          // every Ace counted as 1\n  final int soft;          // one Ace promoted to 11, or == hard when no promotion is possible\n  final boolean isSoft;\n\n  private HandValue(int hard, int soft, boolean isSoft) { this.hard = hard; this.soft = soft; this.isSoft = isSoft; }\n\n  static HandValue of(List<Card> cards) {\n    int hard = 0; boolean hasAce = false;\n    for (Card c : cards) { hard += c.rank().hardValue; if (c.rank().isAce()) hasAce = true; }\n    // At most ONE Ace can ever be 11 — two elevens is 22, always a bust.\n    boolean promotable = hasAce && hard + 10 <= 21;\n    return new HandValue(hard, promotable ? hard + 10 : hard, promotable);\n  }\n\n  int best()      { return isSoft ? soft : hard; }   // the only number the rest of the game needs\n  boolean isBust(){ return hard > 21; }              // bust is decided by the HARD total\n}\n\nfinal class Hand {\n  private final List<Card> cards = new ArrayList<>();\n  private long betCents;\n  private final boolean fromSplit;\n  private boolean doubled = false, surrendered = false, stood = false;\n\n  Hand(long betCents, boolean fromSplit) { this.betCents = betCents; this.fromSplit = fromSplit; }\n\n  void add(Card c) { cards.add(c); }\n  HandValue value() { return HandValue.of(cards); }   // ALWAYS derived, never accumulated\n  int size() { return cards.size(); }\n  long bet() { return betCents; }\n\n  /** A blackjack is the FIRST TWO cards only — and (by common house rule) never after a split. */\n  boolean isBlackjack() {\n    return cards.size() == 2 && !fromSplit && value().best() == 21;\n  }\n  boolean canDouble(HouseRules r) {\n    return cards.size() == 2 && !doubled && (!fromSplit || r.doubleAfterSplit);\n  }\n  boolean canSplit(HouseRules r, int currentHandCount) {\n    return cards.size() == 2\n        && cards.get(0).rank().hardValue == cards.get(1).rank().hardValue   // ten-value pairs count\n        && currentHandCount < r.maxSplits + 1;\n  }\n  boolean isFinished() { return stood || doubled || surrendered || value().isBust(); }\n\n  Card removeSecondForSplit() { return cards.remove(1); }\n  void doubleBet() { betCents *= 2; doubled = true; }\n  void stand() { stood = true; }\n}\n\nfinal class HouseRules {\n  final boolean hitSoftSeventeen, doubleAfterSplit, surrenderAllowed, insuranceOffered;\n  final int maxSplits;\n  final int blackjackPayNum, blackjackPayDen;        // 3:2 or 6:5 — DATA, not a magic constant\n  HouseRules(boolean h17, boolean das, boolean surr, boolean ins, int maxSplits, int num, int den) {\n    hitSoftSeventeen = h17; doubleAfterSplit = das; surrenderAllowed = surr;\n    insuranceOffered = ins; this.maxSplits = maxSplits; blackjackPayNum = num; blackjackPayDen = den;\n  }\n}\n\n/** The dealer has NO choices — only rules. Strategy makes the variant configuration. */\ninterface DealerStrategy { boolean shouldHit(HandValue v); }\n\nfinal class StandardDealerStrategy implements DealerStrategy {\n  private final boolean hitSoft17;\n  StandardDealerStrategy(boolean hitSoft17) { this.hitSoft17 = hitSoft17; }\n  @Override public boolean shouldHit(HandValue v) {\n    int total = v.best();\n    if (total < 17) return true;\n    if (total == 17 && v.isSoft && hitSoft17) return true;   // THE soft-17 rule, in one line\n    return false;\n  }\n}\n\n/** Multi-deck shoe with a cut card. Randomness is INJECTED so games are reproducible. */\nfinal class Shoe {\n  private final List<Card> cards = new ArrayList<>();\n  private final int deckCount; private final Random rng;\n  private int index = 0, cutCard;\n\n  Shoe(int deckCount, Random rng) { this.deckCount = deckCount; this.rng = rng; shuffle(); }\n\n  void shuffle() {\n    cards.clear();\n    for (int d = 0; d < deckCount; d++)\n      for (Suit s : Suit.values()) for (Rank r : Rank.values()) cards.add(new Card(r, s));\n    Collections.shuffle(cards, rng);            // seeded rng => a fully replayable game\n    index = 0;\n    cutCard = (int) (cards.size() * 0.75) + rng.nextInt(10);   // reshuffle point, slightly random\n  }\n\n  Card deal() {\n    if (index >= cards.size()) shuffle();       // safety net; normally the cut card fires first\n    return cards.get(index++);\n  }\n  boolean needsReshuffle() { return index >= cutCard; }   // checked BETWEEN rounds, never mid-hand\n}\n\nfinal class PayoutResolver {\n  private final HouseRules rules;\n  PayoutResolver(HouseRules rules) { this.rules = rules; }\n\n  /** @return net change to the player's bankroll for THIS hand, in cents. */\n  long settle(Hand hand, Hand dealerHand) {\n    if (hand.surrendered()) return -hand.bet() / 2;\n    HandValue p = hand.value(), d = dealerHand.value();\n    if (p.isBust()) return -hand.bet();                       // player busts FIRST — dealer need not play\n    boolean playerBJ = hand.isBlackjack(), dealerBJ = dealerHand.isBlackjack();\n    if (playerBJ && dealerBJ) return 0;                       // push\n    if (playerBJ) return hand.bet() * rules.blackjackPayNum / rules.blackjackPayDen;  // 3:2\n    if (dealerBJ) return -hand.bet();\n    if (d.isBust()) return hand.bet();\n    if (p.best() > d.best()) return hand.bet();\n    if (p.best() < d.best()) return -hand.bet();\n    return 0;                                                 // push — a drawn 21 is NOT a blackjack\n  }\n}"),
      C("deep_dive", "Deep dive: the Ace, and why a running integer cannot work",
        "This is the bug that decides the interview, so treat it as a proof rather than a preference. A card in blackjack has one value except the Ace, which has TWO, and a hand's value is therefore not a number produced by folding an addition over the cards — it is a CHOICE. The naive implementations fail as follows. (1) ACE = 11 ALWAYS: A-A is 22, an instant bust on a hand that is actually 12. Obviously broken. (2) ACE = 1 ALWAYS: A-K is 11, so blackjack itself is unrepresentable and the player never gets paid 3:2. Also obviously broken. (3) ACE = 11 THEN SUBTRACT 10 WHILE BUSTING: this is the version that looks right and is the one candidates actually ship. Trace A-A-9. Start 11 + 11 + 9 = 31; over 21, subtract 10 to get 21. Correct — by accident. Now trace A-A-A-9: 11+11+11+9 = 42; subtract 10 to get 32, still over; subtract 10 again to get 22, still over; subtract 10 a third time to get 12. The right answer IS 12, so the loop stumbles into it — but notice what happened: it needed three separate demotions and it survived only because the loop runs while the total exceeds 21. Now consider what makes the approach genuinely wrong rather than merely ugly: the running total DESTROYS THE INFORMATION the game needs. After it collapses to a single int, nothing can answer 'is this a SOFT 17?' — and the dealer's rule depends on exactly that question. A dealer holding A-6 has 17, and under the hit-soft-17 house rule must draw; a dealer holding 10-7 also has 17 and must stand. Both are the integer 17. A design that only produces an int cannot distinguish them, so soft-17 gets bolted on later as a separate Ace-counting pass — meaning the Ace logic now exists in two places and they will drift. THE CORRECT MODEL is the pair. Count every Ace as 1 to get the HARD total, which is the true minimum and therefore the only correct bust test. Then, if the hand contains at least one Ace and hard + 10 <= 21, one Ace may be promoted to 11, giving the SOFT total. At most one promotion is ever possible, because promoting two Aces adds 20 and any hand with two Aces already has hard >= 2, so hard + 20 >= 22 — always a bust. That is why the pair is complete and there is no need for an ace-count loop at all. Every downstream rule now reads off the pair directly: best() is soft if soft exists else hard; isBust() is hard > 21; isSoft() is exactly the dealer's soft-17 discriminator; and isBlackjack() is size == 2 and best() == 21 and not from a split. One value object, computed from an immutable card list, and the game's most error-prone rule appears exactly once in the codebase."),
      C("deep_dive", "Deep dive: splits, doubles, insurance — why the HAND is the unit of play",
        "The split rule is the requirement that punishes a wrong core model, and interviewers use it deliberately for that reason. Model a Player as having one Hand and everything works right up until a player splits a pair of eights; then the action loop, the bust check, the dealer comparison, and the payout code all need a second hand, and every one of them was written against a single-hand assumption. Model Player as owning List<Hand> from the start and split is a five-line operation: remove the second card from the hand, construct a new Hand seeded with it and flagged fromSplit, deduct a matching bet from the bankroll, deal one fresh card to each hand, and insert the new hand into the play order immediately after the original. The complications that follow are exactly the ones worth discussing unprompted. RESPLITTING: if the new card makes another pair, may the player split again? House rules cap it, usually at four hands total, so maxSplits is configuration and the check consults the CURRENT hand count. SPLIT ACES are almost universally special-cased: each receives exactly one card and cannot be hit further, which is a rule that lives naturally in canHit() on a hand flagged as a split Ace, and nowhere else. THE 21 AFTER A SPLIT is not a blackjack: it pays 1:1, not 3:2 — a distinction worth real money and one of the clearest signals that a candidate knows the domain rather than the shape. DOUBLE DOWN mutates the bet and constrains the future: exactly one more card, then a forced stand, allowed only on a two-card hand, and only after a split if the house permits (doubleAfterSplit). Note the bankroll implication — a player with three split hands who doubles two of them has five bet units at risk, so the model must VALIDATE against the bankroll before allowing the action, and doing that check in one place (a canAfford on Player) rather than at each call site is the design point. INSURANCE is a side bet with its own money and its own resolution: offered only when the dealer shows an Ace, at up to half the original bet, paying 2:1 if the hole card is a ten — which means it resolves BEFORE the main hand, and pushing it into the Hand's bet field would be wrong because it is settled separately and can win while the main hand loses. SURRENDER forfeits half the bet and ends the hand immediately, so the resolver must check it before anything else. All of this is why the phase state machine matters: the legality of an action depends on the phase (you cannot double during DEALER_PLAY), on the hand's card count, on its split provenance, and on the house rules. Centralising that as legalActions(hand, phase, rules) returning a set — rather than scattering the conditions across the UI, the bot, and the controller — is what stops illegal moves reaching the table, and it makes the same logic testable without a UI."),
      C("deep_dive", "Deep dive: the dealer as deterministic strategy, and a reproducible shoe",
        "THE DEALER HAS NO FREE WILL, and encoding that fact is a design opportunity most candidates walk past. Every dealer decision is a pure function of the dealer's own hand and the house rules: draw while the total is below 17; at exactly 17, draw only if the hand is SOFT and the house plays hit-soft-17. That is the entire algorithm, and expressing it as a DealerStrategy interface pays three ways. It makes the soft-17 variant a constructor flag rather than a subclass or a scattered boolean. It makes the dealer independently unit-testable — feed it a HandValue, assert the decision, no table required. And it gives you the seam for the follow-up interviewers love: 'now add a bot player'. A bot is the same shape — a function from (own hand, dealer up card, rules) to an Action — so basic strategy plugs into an interface that already exists, and a card-counting bot differs only by also observing the discard stream. Two rules of dealer sequencing matter and are commonly missed. The HOLE CARD stays face down until players have acted, so any code that lets player logic read it has leaked information and broken the game; the honest model keeps dealer.upCard() as the public query and the full hand private until DEALER_PLAY. And the dealer does not need to play at all if every player hand has busted or surrendered — an optimisation that is also a rule, because a busted player loses even if the dealer would have busted too, which is precisely the house edge and is worth naming as such. THE SHOE is the other half of this deep dive, and it carries the testability requirement. Real blackjack is dealt from 6 or 8 shuffled decks with a CUT CARD placed roughly three quarters of the way down: when the deal pointer passes it, the shoe is reshuffled AFTER the current round completes — never mid-hand, which would be both a rules violation and a subtle source of bugs. Multi-deck matters for correctness of any simulation because it changes the probabilities (two decks contain eight Aces, so the naive assumption that a fresh 52-card deck's odds apply is wrong), and the cut card exists to blunt card counting by leaving a random unknown remainder. Now the design requirement that actually shapes the code: REPRODUCIBILITY. A game whose randomness comes from a private new Random() cannot be tested, cannot be replayed for dispute resolution, and cannot be used to run a Monte Carlo simulation with controlled variance. Inject the Random (or a Shuffler strategy) into the Shoe constructor, and you get three capabilities for free: seeded tests that reproduce a bug exactly, a scripted deterministic shoe for unit tests that need a specific hand such as A-A-9 or a dealer soft 17, and a replay facility where storing the seed plus the action log reconstructs an entire session. Say that out loud — 'randomness is a constructor parameter so games are replayable' — because it is the kind of testability argument that separates a designer from a coder."),
      C("tradeoffs", "Trade-offs & extension points",
        "COMPUTED VALUE vs CACHED VALUE: recomputing HandValue on every query is O(cards), which for a hand of at most eleven cards is free and eliminates a whole class of stale-cache bugs. Caching with invalidation on add() buys nothing here and is the wrong instinct; if a simulation later runs billions of hands, cache it THEN, behind the same method, and measure first. RULE VARIANTS AS CONFIG vs SUBCLASSES: a HouseRules value object composes freely (hit-soft-17 with 6:5 payout and no double-after-split is one object), whereas a subclass per casino combinatorially explodes and cannot express a new mix without new code. This is the standard composition-over-inheritance argument with a concrete cost attached. STRATEGY vs A DEALER SUBCLASS: Strategy keeps the dealer's shape stable and makes both the rule variant and any bot player interchangeable through one interface. EXPLICIT STATE MACHINE vs SCATTERED FLAGS: enumerating phases costs a little ceremony and buys a single place to ask 'is this action legal now', which is where illegal-move bugs would otherwise live; for a two-player toy the ceremony may not pay, and saying so is honest calibration. MONEY REPRESENTATION: integer cents (or BigDecimal) rather than double, because a 3:2 payout on a 5-unit bet and repeated halving on surrender make binary floating point drift — and a gaming system that loses a cent per thousand hands is a real defect. Note that 3:2 on an odd number of cents needs a stated rounding rule, usually in the house's favour. INJECTED RANDOMNESS vs INTERNAL: injection is strictly better and costs one constructor parameter. WHAT BREAKS OR EXTENDS: multi-player at one table is already supported since the round iterates hands; multi-TABLE with a shared shoe is not, and would need either a table-owned shoe (the usual answer) or synchronization on the shoe, at which point the deal pointer becomes a contended resource. Persistence and resumption push you toward event sourcing the action log, which the seeded-shoe design already half provides. Networked play forces the hole card to be genuinely server-side rather than merely private, since a client that receives it can cheat. FURTHER RULE EXTENSIONS the model absorbs without structural change: 6:5 blackjack, European no-hole-card dealing, five-card Charlie, side bets such as 21+3 or perfect pairs (each a separate wager object resolved independently, exactly like insurance), and progressive betting systems on the player side. THE ONE EXTENSION THAT DOES REQUIRE CHANGE is a running-count or shuffle-tracking feature, because it needs the discard stream to be observable — a small addition to Shoe, but one worth naming as the boundary rather than pretending the design covers everything."),
    ],
    "Blackjack is a favourite LLD interview because it looks trivial and hides two traps that separate candidates instantly. The first is the Ace. Interviewers watch for whether you reach for a running integer with a subtract-10 patch, or immediately model value as a hard/soft pair — and then they ask for the dealer's soft-17 rule, which a single int simply cannot answer, forcing the naive design to be rewritten in front of them. The second trap is splitting. If you modelled Player as having one Hand, the split requirement invalidates your action loop, your resolution, and your payouts; if you modelled Player as owning a list of Hands from the start, split is a few lines. Expect follow-ups on the distinction between a blackjack and a drawn 21 (3:2 versus 1:1, and that a post-split 21 is not a blackjack), on where insurance's money lives, on how you would make the dealer's behaviour configurable without subclassing, and on how you would test a hand like A-A-9 or a dealer soft 17 — where the expected answer is an injected Random or a scripted shoe. Strong candidates also volunteer integer cents for money and an explicit phase state machine as the single place that decides which actions are legal.",
    [
      G("running_total_for_ace", "Accumulating a single int for hand value — counting an Ace as 11 and subtracting 10 while over 21 — which collapses the two-valued Ace into one number and makes soft/hard indistinguishable.", "Compute a HandValue PAIR: hard (all Aces as 1) and soft (one Ace promoted to 11 when hard + 10 <= 21). best() is soft if it exists else hard, isBust() tests the HARD total, and isSoft() is exactly the dealer's soft-17 discriminator. At most one Ace can ever be 11, so the pair is complete.", "An aceCount variable with a while (total > 21) total -= 10 loop; a dealer that treats A-6 and 10-7 identically; soft-17 implemented as a second, separate Ace-counting pass."),
      G("player_has_one_hand", "Modelling Player with a single Hand field, so splitting forces a rewrite of the action loop, bust checks, dealer comparison, and payout code.", "Make the HAND the unit of play: Player owns List<Hand>, and every loop, bet, decision and payout is per hand. Split then becomes remove-second-card, new Hand flagged fromSplit, matching bet, one card to each. Resplit limits, split-Ace one-card-only, and double-after-split all become rule checks on the hand.", "player.getHand() returning one object; payout code that reads player.bet(); no fromSplit flag anywhere; splitting bolted on as a special case in the controller."),
      G("blackjack_vs_drawn_21", "Treating any 21 as a blackjack, so a three-card 21 or a post-split 21 gets the 3:2 payout, and paying blackjack odds against a dealer blackjack instead of pushing.", "isBlackjack() is exactly: two cards, total 21, and not from a split. Everything else scoring 21 pays 1:1. Player and dealer both holding blackjack is a PUSH, and the payout ratio itself belongs in HouseRules so 6:5 tables are configuration rather than a code change.", "if (total == 21) payBlackjack(); a hard-coded 1.5 multiplier; no fromSplit check in the blackjack test; payouts computed with double arithmetic instead of integer cents."),
    ],
    0.6, DIAG.blackjack, "Blackjack class diagram"),
];

const xpFor = (l) => (l === "warmup" ? 5 : l === "easy" ? 10 : l === "medium" ? 15 : 20);
const diffFor = (l) => (l === "warmup" ? 0.1 : l === "easy" ? 0.2 : l === "medium" ? 0.4 : 0.6);
function pm(moduleId, o) {
  return { trackKey: TRACK_KEY, moduleId, topicId: o.topicId, exerciseId: o.exerciseId,
    position: o.position, level: o.level, type: "pattern_match", title: o.title, scenario: o.scenario,
    instructions: "Pick the best answer.", starterCode: "", expectedSolution: o.correct,
    blanks: [{ options: o.options }], testCases: [{ type: "pattern_match", correct: o.correct, explanation: o.explanation }],
    hints: [], xpReward: xpFor(o.level), difficulty: diffFor(o.level) };
}

const EXERCISES = [
  // Event loop
  pm("lld_m20", { topicId: "lld_m20_t3", exerciseId: "lld_m20_t3_pm_d1", position: 11, level: "hard",
    title: "Timers fire 100 ms late", scenario: "Your loop calls selector.select(100) every iteration and then checks the timer heap. A timer scheduled for 5 ms fires up to 100 ms late, and an idle process still burns CPU. What is the fix?",
    options: ["Derive the select timeout each iteration as clamp(earliestTimerDeadline - now, 0, idleCap), and force it to 0 when the task queue is already non-empty", "Check the timer heap before calling select instead of after", "Run the timer heap on a second thread so it is not blocked by select", "Lower the constant timeout to 1 ms so drift stays under a millisecond"],
    correct: "Derive the select timeout each iteration as clamp(earliestTimerDeadline - now, 0, idleCap), and force it to 0 when the task queue is already non-empty",
    explanation: "While the thread is blocked in select nothing else can fire a timer, so the sleep duration IS the timer resolution. A min-heap gives the nearest deadline in O(1) via peek, so the correct timeout is free to compute. A 1 ms constant only trades drift for ten times the idle wakeups; a second thread throws away the lock-free single-thread property." }),
  pm("lld_m20", { topicId: "lld_m20_t3", exerciseId: "lld_m20_t3_pm_d2", position: 12, level: "hard",
    title: "One slow callback froze 50,000 clients", scenario: "A single handler does a 400 ms synchronous database call. Every connection on the server stalls for 400 ms. What does the design have to say about this?",
    options: ["Callbacks run to completion and cannot be pre-empted, so blocking or CPU-heavy work must be offloaded to a worker pool and the result returned via loop.execute(() -> continuation(result))", "Add more event loops so the slow callback is amortised across them", "Wrap the handler in a synchronized block so other handlers wait cleanly", "Increase the select timeout so the loop has more time to catch up"],
    correct: "Callbacks run to completion and cannot be pre-empted, so blocking or CPU-heavy work must be offloaded to a worker pool and the result returned via loop.execute(() -> continuation(result))",
    explanation: "Single-threaded run-to-completion is what buys the lock-free design, and the bill is that any callback stalls everything. The worker must hand results back through execute() rather than touching loop-owned state, or the confinement argument collapses. Blocking on a future completed by a task on the same loop is an unrecoverable self-deadlock." }),
  pm("lld_m20", { topicId: "lld_m20_t3", exerciseId: "lld_m20_t3_pm_d3", position: 13, level: "medium",
    title: "Microtask vs setTimeout(f, 0)", scenario: "Inside one callback you resolve a promise (queuing a microtask) and also call setTimeout(f, 0). Which runs first, and why is that the design?",
    options: ["The microtask: the loop drains the microtask queue to empty after EVERY macro-task, so a zero-delay timer is still a macro-task scheduled for a later iteration", "The timer: a 0 ms delay means immediately, which beats a deferred microtask", "Whichever was queued first, since both go on the same FIFO queue", "They run concurrently on the loop thread and the order is undefined"],
    correct: "The microtask: the loop drains the microtask queue to empty after EVERY macro-task, so a zero-delay timer is still a macro-task scheduled for a later iteration",
    explanation: "Microtasks exist to finish a logical operation atomically before anything else is observed; macro-tasks are the loop's scheduling unit. setTimeout(f, 0) means at-or-after now on a future iteration, never now. The cost of the guarantee is that a microtask which re-queues itself starves all macro-tasks — hence the drain guard." }),
  // Latch / barrier
  pm("lld_m20", { topicId: "lld_m20_t4", exerciseId: "lld_m20_t4_pm_d1", position: 11, level: "hard",
    title: "The barrier that hangs after 4,000 rounds", scenario: "A cyclic barrier waits while (count > 0). The last arriver resets count to parties and calls notifyAll. Occasionally the simulation hangs. What is the defect?",
    options: ["Count is reused, so it is not a valid predicate: a woken thread reacquires the lock after the reset, re-tests count > 0 as true, and waits again — the barrier needs a GENERATION object whose identity names the round", "notifyAll should be called before resetting the count", "The count should be volatile so waiters see the reset", "wait() should be replaced with wait(timeout) so a missed signal self-heals"],
    correct: "Count is reused, so it is not a valid predicate: a woken thread reacquires the lock after the reset, re-tests count > 0 as true, and waits again — the barrier needs a GENERATION object whose identity names the round",
    explanation: "The last arriver can finish its next slice and re-enter await() before a woken thread reacquires the monitor. That thread then sees a count belonging to round N+1 and goes back to sleep, permanently one arrival short. The generation predicate (generation != myGen) is monotonic per waiter, so no later round can push it back into waiting." }),
  pm("lld_m20", { topicId: "lld_m20_t4", exerciseId: "lld_m20_t4_pm_d2", position: 12, level: "medium",
    title: "notify or notifyAll when the latch opens", scenario: "countDown() calls notify() when the count hits zero. The unit test with one waiter passes; production deadlocks with three waiters. Why is notifyAll required here?",
    options: ["An open latch is a state change all waiters must observe, not a consumable permit handed to one thread — notify() releases exactly one waiter and strands the rest forever", "notify() is fine but must be called twice for three waiters", "notify() only works with ReentrantLock, not with intrinsic monitors", "The count should be decremented after the notify so the last waiter sees zero"],
    correct: "An open latch is a state change all waiters must observe, not a consumable permit handed to one thread — notify() releases exactly one waiter and strands the rest forever",
    explanation: "notify() is only safe when waiters are homogeneous AND the signal is a consumable resource. Reaching zero opens the gate permanently for everyone, so all waiters must be released. The thundering-herd cost of notifyAll is the price of correctness; the alternative is explicit Condition objects, not notify()." }),
  pm("lld_m20", { topicId: "lld_m20_t4", exerciseId: "lld_m20_t4_pm_d3", position: 13, level: "hard",
    title: "One party is interrupted mid-rendezvous", scenario: "Eight workers rendezvous each timestep. Worker 5 is interrupted inside await() and simply throws and returns. Shutdown now hangs with seven threads parked. What does the design owe here?",
    options: ["Broken-barrier semantics: the departing thread sets broken, restores count and notifyAll()s, so the other seven wake and throw BrokenBarrierException instead of waiting for an arrival that will never come", "The other seven should use a timed await and retry the round when it expires", "The barrier should automatically re-register a replacement thread for the missing party", "Interrupts should be swallowed inside await() so the rendezvous always completes"],
    correct: "Broken-barrier semantics: the departing thread sets broken, restores count and notifyAll()s, so the other seven wake and throw BrokenBarrierException instead of waiting for an arrival that will never come",
    explanation: "An all-or-nothing rendezvous must fail all-or-nothing. Note the deliberate asymmetry: the interrupted thread throws InterruptedException, the collateral waiters throw BrokenBarrierException. A throwing barrierAction must break the barrier in a finally block for the same reason. Swallowing the interrupt makes the thread uncancellable; Phaser is the answer when the party count genuinely varies." }),
  // Lock-free stack
  pm("lld_m20", { topicId: "lld_m20_t5", exerciseId: "lld_m20_t5_pm_d1", position: 11, level: "hard",
    title: "A successful CAS that corrupts the stack", scenario: "Stack is A -> B -> C. T1 reads head=A and next=B, then stalls. T2 pops A, pops B, and pushes A back. T1 resumes and CASes head from A to B — and it succeeds. What happened?",
    options: ["ABA: CAS compares the pointer, not the history, so it cannot distinguish unchanged from changed-and-restored — T1 installs the stale successor B, resurrecting a popped node and losing A", "T1 read a torn pointer because next was not volatile", "The CAS should have failed; this indicates a JVM memory-model bug", "T2 violated linearizability by pushing a node it had just popped"],
    correct: "ABA: CAS compares the pointer, not the history, so it cannot distinguish unchanged from changed-and-restored — T1 installs the stale successor B, resurrecting a popped node and losing A",
    explanation: "The algorithm needs 'unmodified since I read it' but CAS only answers 'still equal to what I read'. Fixes: version the pointer (AtomicStampedReference, or tag bits in native code), or use hazard pointers / epoch reclamation. Java's GC removes the use-after-free half of the problem but not ABA — recycle nodes through a pool and this interleaving is live." }),
  pm("lld_m20", { topicId: "lld_m20_t5", exerciseId: "lld_m20_t5_pm_d2", position: 12, level: "medium",
    title: "Lock-free is not wait-free", scenario: "A candidate says the Treiber stack guarantees every thread finishes in bounded time because there is no lock. What is the precise guarantee?",
    options: ["Lock-free: at least ONE thread completes in bounded steps system-wide, because a CAS fails only when another thread's CAS succeeded — an individual thread may retry indefinitely", "Wait-free: no thread ever blocks, so all threads are bounded", "Obstruction-free: progress only when a thread runs in isolation", "Linearizable, which implies per-thread progress bounds"],
    correct: "Lock-free: at least ONE thread completes in bounded steps system-wide, because a CAS fails only when another thread's CAS succeeded — an individual thread may retry indefinitely",
    explanation: "My failure is evidence of someone else's success, so the system cannot stall — but there is no per-thread bound and individual starvation is permitted by the contract. Wait-freedom needs a helping protocol where threads complete each other's announced operations, and it costs far more in the common case." }),
  pm("lld_m20", { topicId: "lld_m20_t5", exerciseId: "lld_m20_t5_pm_d3", position: 13, level: "hard",
    title: "32 threads, and it is slower than the lock", scenario: "Your lock-free stack benchmarks slower than the synchronized version at 32 threads. The CAS itself is a single instruction. What is actually happening, and what is the structural fix?",
    options: ["Every thread hammers ONE cache line for head, so it ping-pongs between cores and the failure rate climbs superlinearly — mitigate with exponential randomized backoff, and structurally with an elimination array where a failed push and a failed pop exchange directly without touching head", "The AtomicReference wrapper allocates on every operation, so the fix is a plain volatile field", "CAS is implemented as a lock on x86, so the two designs are equivalent", "Retry loops trigger extra GC, so the fix is a larger young generation"],
    correct: "Every thread hammers ONE cache line for head, so it ping-pongs between cores and the failure rate climbs superlinearly — mitigate with exponential randomized backoff, and structurally with an elimination array where a failed push and a failed pop exchange directly without touching head",
    explanation: "Lock-free removes blocking, not contention. The randomization in the backoff matters: without it threads retry in lockstep and collide again. Elimination is the real fix because a matched push/pop pair can be linearized adjacently, so throughput rises with contention instead of collapsing." }),
  // Token bucket
  pm("lld_m20", { topicId: "lld_m20_t6", exerciseId: "lld_m20_t6_pm_d1", position: 11, level: "hard",
    title: "40,000 keys, 40,000 timers", scenario: "Each bucket schedules a repeating task that adds a token every 10 ms. At 40,000 API keys the scheduler saturates, idle keys keep ticking, and the bucket map never shrinks. What is the correct mechanism?",
    options: ["Derive the refill on read from elapsed time — tokens = min(capacity, tokens + (now - lastRefillNanos) * ratePerNano) — so an idle bucket costs zero and settles its whole debt in one multiply when touched", "Share one scheduled task that iterates every bucket each tick", "Increase the tick period to 1 second so the scheduler does less work", "Use a timer wheel so scheduling each bucket is O(1) instead of O(log n)"],
    correct: "Derive the refill on read from elapsed time — tokens = min(capacity, tokens + (now - lastRefillNanos) * ratePerNano) — so an idle bucket costs zero and settles its whole debt in one multiply when touched",
    explanation: "Pushing tokens in makes cost proportional to KEYS instead of traffic, quantizes grants to the tick period, and keeps a live reference that blocks eviction. Deriving them inverts that: no thread, no wakeup, exact accrual. The min() clamp is what makes an arbitrarily long idle period safe instead of banking three days of tokens." }),
  pm("lld_m20", { topicId: "lld_m20_t6", exerciseId: "lld_m20_t6_pm_d2", position: 12, level: "hard",
    title: "Two threads, one token, two grants", scenario: "tryAcquire does refill(); if (tokens >= 1) { tokens -= 1; return true; }. With one token left, two threads occasionally both get true. What is the fix, and why must the two state fields move together?",
    options: ["Fuse refill + test + consume into ONE atomic transition — a synchronized block, or a CAS over an immutable (tokens, lastRefillNanos) snapshot, because updating the two fields separately leaves a window where no reader can tell which half is current", "Make tokens an AtomicLong and use decrementAndGet, checking for a negative result", "Make both fields volatile so each write is visible immediately", "Refill on a background thread so the hot path only decrements"],
    correct: "Fuse refill + test + consume into ONE atomic transition — a synchronized block, or a CAS over an immutable (tokens, lastRefillNanos) snapshot, because updating the two fields separately leaves a window where no reader can tell which half is current",
    explanation: "It is a read-modify-write; volatile gives visibility, never atomicity. decrementAndGet can drive the count negative and breaks the all-or-nothing contract for multi-permit requests. Bundling both fields into one immutable object reduces a two-field update to a one-word CAS — that is exactly why the snapshot is immutable." }),
  pm("lld_m20", { topicId: "lld_m20_t6", exerciseId: "lld_m20_t6_pm_d3", position: 13, level: "medium",
    title: "The limiter wedged for two seconds", scenario: "The bucket stores lastRefillMillis from System.currentTimeMillis. After an NTP correction stepped the clock back two seconds, the limiter refused every request until wall time caught up. What is the root cause?",
    options: ["Elapsed time must come from a MONOTONIC clock (System.nanoTime): wall clock can step backwards, making elapsed negative, and forwards, instantly crediting unearned tokens", "The refill rate was configured in seconds instead of milliseconds", "currentTimeMillis has only millisecond resolution, which truncates the token accrual", "The bucket should have been reset when the clock changed"],
    correct: "Elapsed time must come from a MONOTONIC clock (System.nanoTime): wall clock can step backwards, making elapsed negative, and forwards, instantly crediting unearned tokens",
    explanation: "Only differences matter here, which is exactly what nanoTime provides (its origin is arbitrary, and the subtraction is safe even across the long overflow boundary). Inject it as a LongSupplier so the limiter is unit-testable without sleeping. Note nanoTime is monotonic within one JVM only, which is why a distributed limiter must keep its clock server-side." }),
  // Actor system
  pm("lld_m20", { topicId: "lld_m20_t8", exerciseId: "lld_m20_t8_pm_d1", position: 11, level: "hard",
    title: "Why the fields need no synchronization", scenario: "An actor's state is plain, non-volatile, unsynchronized fields, yet 100,000 actors run on 8 threads. What guarantee makes that safe?",
    options: ["Isolation (no reference to the actor escapes — callers hold only an ActorRef) PLUS the runtime invariant that at most ONE thread executes a given actor's receive() at any instant, enforced by a per-actor scheduled flag flipped with CAS", "The mailbox is thread-safe, so everything downstream of it is thread-safe too", "Messages are immutable, which makes the receiver's state immutable as well", "The dispatcher pins each actor to one thread for the actor's whole lifetime"],
    correct: "Isolation (no reference to the actor escapes — callers hold only an ActorRef) PLUS the runtime invariant that at most ONE thread executes a given actor's receive() at any instant, enforced by a per-actor scheduled flag flipped with CAS",
    explanation: "Isolation alone is just the premise: without serial execution two pool threads could pick up two messages for the same actor and race on the same fields. Pinning would not scale to 100,000 actors. Visibility across consecutive dispatch slices comes free from the mailbox's happens-before edge." }),
  pm("lld_m20", { topicId: "lld_m20_t8", exerciseId: "lld_m20_t8_pm_d2", position: 12, level: "hard",
    title: "Eight slow queries stalled 100,000 actors", scenario: "A few actors call a JDBC API inside receive(). One slow database makes the entire system unresponsive, even for actors that never touch the database. Why, and what is the fix?",
    options: ["Blocking inside receive() occupies a SHARED dispatcher thread, so a handful of blocked actors exhaust the pool and nobody is left to run the rest — use async APIs and pipe the result back to self as a message, or isolate blocking actors on a dedicated dispatcher", "The mailbox lock is held during receive(), so other actors block on it", "The dispatcher processes actors in strict round-robin, so one slow actor delays the whole rotation", "Garbage collection pauses lengthen while threads are blocked on I/O"],
    correct: "Blocking inside receive() occupies a SHARED dispatcher thread, so a handful of blocked actors exhaust the pool and nobody is left to run the rest — use async APIs and pipe the result back to self as a message, or isolate blocking actors on a dedicated dispatcher",
    explanation: "The blocked actor is not the victim; the thread it is holding is. Piping is the standard fix, and the completion handler must send a message rather than touch actor state directly, or serial execution is broken. Beware ask(...).get() inside an actor — the responder may be waiting for a thread you are holding." }),
  pm("lld_m20", { topicId: "lld_m20_t8", exerciseId: "lld_m20_t8_pm_d3", position: 13, level: "medium",
    title: "What restart actually preserves", scenario: "A child actor throws and the parent's strategy returns RESTART. What survives, and why does that matter to everyone holding a reference to it?",
    options: ["The ActorRef and the MAILBOX survive while the actor instance and its corrupted state are discarded and rebuilt from the factory — so queued messages are processed by the fresh instance and no sender ever learns a restart happened", "Everything survives including the instance state; only the failed message is dropped", "The actor instance survives but the mailbox is cleared so the poison message cannot recur", "The actor is stopped and callers must obtain a new ActorRef from the registry"],
    correct: "The ActorRef and the MAILBOX survive while the actor instance and its corrupted state are discarded and rebuilt from the factory — so queued messages are processed by the fresh instance and no sender ever learns a restart happened",
    explanation: "Stable identity is what makes let-it-crash usable: failure handling is located in the supervision tree rather than scattered across catch blocks. RESUME keeps state and drops the message; STOP dead-letters the remainder; ESCALATE hands the decision to the grandparent. Add exponential backoff or a persistent failure becomes a restart storm." }),
  // Blackjack
  pm("lld_m21", { topicId: "lld_m21_t1", exerciseId: "lld_m21_t1_pm_d1", position: 11, level: "hard",
    title: "The dealer holds A-6", scenario: "Hand value is a running int: count each Ace as 11, then subtract 10 while over 21. The house plays hit-soft-17. Why can this model never implement that rule?",
    options: ["A single int cannot distinguish soft 17 (A-6) from hard 17 (10-7) — model value as a PAIR: hard with all Aces as 1, and soft = hard + 10 when hard + 10 <= 21", "The subtraction loop should subtract 11 rather than 10 for the first Ace", "Soft 17 requires tracking the ORDER the cards were dealt, which the running total discards", "It can, by re-scanning the cards for an Ace whenever the total equals 17"],
    correct: "A single int cannot distinguish soft 17 (A-6) from hard 17 (10-7) — model value as a PAIR: hard with all Aces as 1, and soft = hard + 10 when hard + 10 <= 21",
    explanation: "The running total destroys the information the rule needs. With the pair, isSoft() IS the discriminator and the whole rule is one line. At most one Ace can ever count 11 (two would be 22), so the pair is complete — no ace-count loop is needed. Bust is tested against the HARD total." }),
  pm("lld_m21", { topicId: "lld_m21_t1", exerciseId: "lld_m21_t1_pm_d2", position: 12, level: "hard",
    title: "A player splits a pair of eights", scenario: "Your Player class has a single Hand field. The split requirement arrives. What does the correct core model look like, and why does it matter beyond splitting?",
    options: ["The HAND is the unit of play: Player owns List<Hand>, and bets, decisions, busts and payouts are all per hand — split then just moves the second card into a new hand with its own matching bet", "Give Player a second optional hand field and branch on whether it is present", "Represent the split as two Player objects sharing one bankroll", "Keep one hand and store the split cards in a side list, resolving them after the main hand"],
    correct: "The HAND is the unit of play: Player owns List<Hand>, and bets, decisions, busts and payouts are all per hand — split then just moves the second card into a new hand with its own matching bet",
    explanation: "A one-hand model forces a rewrite of the action loop, resolution and payouts. With a list, resplit limits, split-Ace one-card-only, and double-after-split all become rule checks on a hand. Note the bankroll consequence: three split hands with two doubled puts five bet units at risk, so affordability must be validated in one place." }),
  pm("lld_m21", { topicId: "lld_m21_t1", exerciseId: "lld_m21_t1_pm_d3", position: 13, level: "medium",
    title: "Which 21 pays 3:2", scenario: "A player draws to 21 in three cards, and another makes 21 on a split hand. Both get paid 3:2 by your resolver. What is the correct rule?",
    options: ["isBlackjack() is exactly two cards totalling 21 and NOT from a split; every other 21 pays 1:1, dual blackjacks push, and the payout ratio itself belongs in HouseRules so 6:5 tables are configuration", "Any 21 is a blackjack; the 3:2 payout is the standard for the total, not for the card count", "A three-card 21 pays 3:2 but a split 21 pays 1:1", "Blackjack pays 3:2 only when the dealer does not also hold 21, otherwise the player loses"],
    correct: "isBlackjack() is exactly two cards totalling 21 and NOT from a split; every other 21 pays 1:1, dual blackjacks push, and the payout ratio itself belongs in HouseRules so 6:5 tables are configuration",
    explanation: "The distinction is worth real money and is a standard domain-knowledge probe. Two blackjacks push rather than the player losing. Keep the ratio as data (numerator/denominator) and settle in integer cents — a hard-coded 1.5 multiplier on a double both hard-codes the house rule and drifts financially." }),
];

async function run() {
  await mongoose.connect(process.env.MONGO_URI);
  const moduleIds = [...new Set(TOPICS.map((t) => t.moduleId))];
  for (const mid of moduleIds) {
    const m = await ProModule.findOne({ trackKey: TRACK_KEY, moduleId: mid }).lean();
    if (!m) throw new Error(`Module ${mid} not found — run the base LLD seeds first.`);
  }
  let tUp = 0, eUp = 0;
  for (const t of TOPICS) { await ProTopic.updateOne({ trackKey: TRACK_KEY, topicId: t.topicId }, { $set: t }, { upsert: true }); tUp++; }
  for (const e of EXERCISES) { await ProExercise.updateOne({ trackKey: TRACK_KEY, exerciseId: e.exerciseId }, { $set: e }, { upsert: true }); eUp++; }
  const totals = await recomputeTrackTotals(TRACK_KEY);
  console.log(`✓ LLD depth batch 19 seeded: ${tUp} topics deepened, ${eUp} exercises upserted.`);
  console.log(`  track totals now: ${JSON.stringify(totals)}`);
  await mongoose.disconnect();
}
run().catch((err) => { console.error(err); process.exit(1); });
